---
phase: 08-dashboard-notificaciones-reportes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/notificaciones/page.tsx
autonomous: true
requirements: [NOTF-01, NOTF-02, NOTF-03, NOTF-04, NOTF-05]

must_haves:
  truths:
    - "Wizard step 1 shows a list of etiquetas to select from, and Siguiente is disabled until one is selected"
    - "Wizard step 2 shows paquetes filtered by the selected etiqueta"
    - "Wizard step 3 lets user select individual paquetes via checkboxes, Siguiente disabled until at least one selected"
    - "Wizard step 4 shows an email preview card with selected paquetes, their destino, and precio venta"
    - "Wizard step 5 sends (simulated) and shows a success toast, then resets wizard to step 1"
  artifacts:
    - path: "src/app/(admin)/notificaciones/page.tsx"
      provides: "5-step notification wizard with etiqueta selection, paquete filtering, email preview, simulated send"
      min_lines: 200
  key_links:
    - from: "src/app/(admin)/notificaciones/page.tsx"
      to: "CatalogProvider"
      via: "useEtiquetas() for step 1 etiqueta list"
      pattern: "useEtiquetas\\(\\)"
    - from: "src/app/(admin)/notificaciones/page.tsx"
      to: "PackageProvider"
      via: "usePaquetes() + usePackageState().paqueteEtiquetas for step 2 filtering"
      pattern: "usePaquetes\\(\\)|usePackageState\\(\\)"
    - from: "src/app/(admin)/notificaciones/page.tsx"
      to: "ServiceProvider"
      via: "useAereos() for destino lookup in email preview"
      pattern: "useAereos\\(\\)"
    - from: "src/app/(admin)/notificaciones/page.tsx"
      to: "Toast"
      via: "useToast() for success notification on send"
      pattern: "useToast\\(\\)|toast\\("
---

<objective>
Build the 5-step Notificaciones wizard that walks through etiqueta selection, paquete filtering, paquete selection, email preview, and simulated send with success toast.

Purpose: The notification wizard is the key workflow for sending paquete updates to vendedores -- it demonstrates the multi-step form pattern and data aggregation across providers.
Output: Fully functional 5-step wizard at /notificaciones.
</objective>

<execution_context>
@/Users/diegothx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diegothx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-dashboard-notificaciones-reportes/08-RESEARCH.md
@src/components/providers/CatalogProvider.tsx
@src/components/providers/PackageProvider.tsx
@src/components/providers/ServiceProvider.tsx
@src/components/ui/Card.tsx
@src/components/ui/Badge.tsx
@src/components/ui/Checkbox.tsx
@src/components/ui/Button.tsx
@src/components/ui/Toast.tsx
@src/components/layout/PageHeader.tsx
@src/components/lib/glass.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build wizard step indicator and navigation shell</name>
  <files>src/app/(admin)/notificaciones/page.tsx</files>
  <action>
Replace the stub `notificaciones/page.tsx` with a full `"use client"` wizard page.

**Wizard state management:**
- `const [step, setStep] = useState<1 | 2 | 3 | 4 | 5>(1);`
- `const [selectedEtiquetaId, setSelectedEtiquetaId] = useState<string | null>(null);`
- `const [selectedPaqueteIds, setSelectedPaqueteIds] = useState<Set<string>>(new Set());`
- `const [isSending, setIsSending] = useState(false);`

**Data hooks:**
- `const etiquetas = useEtiquetas();` from CatalogProvider
- `const paquetes = usePaquetes();` from PackageProvider (brand-filtered)
- `const state = usePackageState();` from PackageProvider (for paqueteEtiquetas junction)
- `const aereos = useAereos();` from ServiceProvider (for destino lookup in email preview)
- `const { toast } = useToast();` from Toast

**Derived data:**
- `filteredPaquetes`: filter paquetes where `state.paqueteEtiquetas.some(pe => pe.paqueteId === p.id && pe.etiquetaId === selectedEtiquetaId)`. Also filter out deleted paquetes.
- `aereoMap`: `Record<string, string>` mapping `aereoId -> destino` built from `aereos`.
- `getDestinoForPaquete(paqueteId)`: find first `state.paqueteAereos` entry for this paquete, look up destino from aereoMap. Fallback to "Sin destino".

**Step indicator:**
- Horizontal row of 5 circles, each numbered 1-5. Active step: teal background (`bg-teal-500 text-white`), completed steps: teal with checkmark, upcoming: `bg-neutral-200 text-neutral-400`.
- Below each circle, a small label: "Etiqueta", "Paquetes", "Seleccion", "Preview", "Enviar".
- Connecting lines between circles: teal for completed, neutral-200 for upcoming.
- Wrap in a `<Card variant="default">` with padding.

**Navigation buttons:**
- Bottom of the wizard card: flex row with "Atras" button (secondary/outline, disabled on step 1) and "Siguiente" button (primary, disabled based on step validation).
- Step 1: Siguiente disabled if `!selectedEtiquetaId`.
- Step 2: Siguiente always enabled (viewing only).
- Step 3: Siguiente disabled if `selectedPaqueteIds.size === 0`.
- Step 4: Show "Enviar Notificaciones" button instead of Siguiente.
- Step 5: No navigation -- shows sending state then resets.
- Use `<Button>` from `@/components/ui/Button`.

**Step content area:**
- Below the step indicator, inside the same Card, render step content conditionally: `{step === 1 && <Step1Content />}` etc.
- Each step content is an inline function/section, NOT separate component files (keep everything in one page file for simplicity).

**Step 1 -- Select Etiqueta (NOTF-01):**
- Heading: "Selecciona una etiqueta" in text-sm font-semibold text-neutral-700 mb-3.
- Render etiquetas as clickable cards in a `grid grid-cols-2 sm:grid-cols-3 gap-3`.
- Each etiqueta card: a div with `border-2` that changes to `border-teal-500` when selected (`selectedEtiquetaId === et.id`). Show a small color dot (circle with `style={{ backgroundColor: et.color }}`), etiqueta name, and count of associated paquetes.
- Clicking selects/deselects that etiqueta.

**Step 2 -- View Filtered Paquetes (NOTF-02):**
- Heading: "Paquetes con etiqueta: {selectedEtiquetaName}" in text-sm font-semibold.
- Render `filteredPaquetes` in a list/table showing: titulo, destino (derived from aereoMap), estado badge, precioVenta formatted with `formatCurrency`.
- If no paquetes match, show "No hay paquetes con esta etiqueta" message.
- Import `formatCurrency` from `@/lib/utils`.

**Step 3 -- Select Paquetes (NOTF-03):**
- Heading: "Selecciona paquetes a notificar" with a "Seleccionar todos" checkbox at top.
- Render `filteredPaquetes` with a `<Checkbox>` per row. Checked state from `selectedPaqueteIds.has(p.id)`.
- Toggle individual: add/remove from Set. Toggle all: if all selected, clear; else add all.
- Show count: "{N} de {M} seleccionados" in text-xs text-neutral-400.

**Step 4 -- Email Preview (NOTF-04):**
- Heading: "Vista previa del email".
- Render a simulated email card inside a `<Card variant="default">` with white background, styled to look like an email.
- Email layout: "header" area with brand-colored bar, then heading "Nuevos paquetes disponibles", then for each selected paquete: titulo (bold), destino, `precioVenta` formatted, and a fake "Ver paquete" link button (styled as inline block with teal background, does nothing).
- Footer: "Este email sera enviado a los vendedores asociados a la marca activa."
- Use `glassMaterials.frostedSubtle` for the email preview card background.

**Step 5 -- Send (NOTF-05):**
- Handled by the "Enviar Notificaciones" button on step 4.
- When clicked: `setIsSending(true)`, then after a simulated 1.5s delay (`setTimeout`), call `toast("success", "Notificaciones enviadas", \`${selectedPaqueteIds.size} paquetes notificados exitosamente\`)`, then reset ALL wizard state: `setStep(1)`, `setSelectedEtiquetaId(null)`, `setSelectedPaqueteIds(new Set())`, `setIsSending(false)`.
- While sending, show a simple "Enviando..." text with a subtle opacity animation and disable the button.

**Imports:**
- `useState, useMemo` from `react`
- `useEtiquetas` from `@/components/providers/CatalogProvider`
- `usePaquetes, usePackageState` from `@/components/providers/PackageProvider`
- `useAereos` from `@/components/providers/ServiceProvider`
- `useToast` from `@/components/ui/Toast`
- `Card, CardContent, CardHeader` from `@/components/ui/Card`
- `Badge` from `@/components/ui/Badge`
- `Checkbox` from `@/components/ui/Checkbox`
- `Button` from `@/components/ui/Button`
- `PageHeader` from `@/components/layout/PageHeader`
- `formatCurrency` from `@/lib/utils`
- `glassMaterials` from `@/components/lib/glass`
- `Check, ChevronRight, ChevronLeft, Send, Tag, Mail` from `lucide-react`

**Page structure:**
- `<PageHeader title="Notificaciones" subtitle="Envio de notificaciones a vendedores" />`
- Single `<Card variant="default">` containing: step indicator -> step content -> navigation buttons.
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Verify the file exports a default function component and uses all required hooks.</verify>
  <done>Notificaciones page renders a 5-step wizard: step 1 shows clickable etiqueta cards, step 2 shows filtered paquetes, step 3 has checkboxes for selection, step 4 shows email preview with paquete details, and the send button triggers a simulated send with toast and wizard reset.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Step 1: Etiqueta cards render with correct colors, Siguiente disabled until selection
- Step 2: Only paquetes with selected etiqueta appear
- Step 3: Checkboxes toggle correctly, "Select all" works, Siguiente disabled until selection
- Step 4: Email preview shows selected paquetes with titulo, destino, precio
- Send: Toast appears, wizard resets to step 1 with all state cleared
- Atras button navigates backward, disabled on step 1
- Glass design system used throughout (Card, Badge, Checkbox, Button)
</verification>

<success_criteria>
1. Wizard step 1 shows all brand-filtered etiquetas as clickable cards with color dots
2. Wizard step 2 correctly filters paquetes by selected etiqueta using paqueteEtiquetas junction
3. Wizard step 3 enables individual and "select all" checkbox selection
4. Wizard step 4 renders a realistic email preview with service summary for selected paquetes
5. Send action shows success toast and resets wizard completely
6. Step indicator shows progress with teal active states and connecting lines
7. Navigation guards prevent advancing without required selections
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-notificaciones-reportes/08-02-SUMMARY.md`
</output>
