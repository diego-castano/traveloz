---
phase: 08-dashboard-notificaciones-reportes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/reportes/page.tsx
autonomous: true
requirements: [REPT-01, REPT-02, REPT-03]

must_haves:
  truths:
    - "Reportes page shows 4 stat cards: Paquetes Activos, Aereos, Alojamientos, Visitas Web (simulated)"
    - "Reportes page renders a recharts BarChart showing paquetes count by destino"
    - "Reportes page renders a table of most-used hotels with hotel name and usage count"
  artifacts:
    - path: "src/app/(admin)/reportes/page.tsx"
      provides: "Reports page with stat cards, bar chart, and hoteles table"
      min_lines: 150
  key_links:
    - from: "src/app/(admin)/reportes/page.tsx"
      to: "PackageProvider"
      via: "usePaquetes() + usePackageState() for aggregated stats and chart data"
      pattern: "usePaquetes\\(\\)|usePackageState\\(\\)"
    - from: "src/app/(admin)/reportes/page.tsx"
      to: "ServiceProvider"
      via: "useAereos() + useAlojamientos() for destino join and hotel names"
      pattern: "useAereos\\(\\)|useAlojamientos\\(\\)"
    - from: "src/app/(admin)/reportes/page.tsx"
      to: "recharts"
      via: "BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer"
      pattern: "BarChart|ResponsiveContainer"
    - from: "src/app/(admin)/reportes/page.tsx"
      to: "motion/react"
      via: "animate(useMotionValue, target) for stat card counter animation"
      pattern: "useMotionValue|animate"
---

<objective>
Build the Reportes page with 4 animated stat cards, a recharts bar chart of paquetes by destino, and a glass table of most-used hotels.

Purpose: The reports page provides data visualization for the admin panel, aggregating data from packages, flights, and hotels into meaningful charts and tables.
Output: Full reports page at /reportes with working chart and table.
</objective>

<execution_context>
@/Users/diegothx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diegothx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-dashboard-notificaciones-reportes/08-RESEARCH.md
@src/components/ui/Card.tsx
@src/components/ui/Table.tsx
@src/components/lib/glass.ts
@src/components/lib/animations.ts
@src/components/providers/PackageProvider.tsx
@src/components/providers/ServiceProvider.tsx
@src/components/providers/AuthProvider.tsx
@src/components/layout/PageHeader.tsx
@src/app/(admin)/paquetes/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Reportes page with stat cards and animated counters</name>
  <files>src/app/(admin)/reportes/page.tsx</files>
  <action>
Replace the stub `reportes/page.tsx` with a full `"use client"` reports page.

**AnimatedCounter component (reusable inline component, same pattern as Dashboard):**
- Accept `{ icon, label, value, color }` props.
- Use `useMotionValue(0)`, `useTransform(count, Math.round)`, `animate(count, value, { duration: 1.2, ease: "easeOut" })` with cleanup.
- Render inside `<Card variant="stat"><CardContent>` with `<StatIcon>`.
- Same styling as Dashboard stat cards: label in `text-xs text-neutral-400 font-medium uppercase tracking-wide mb-1`, number in `<motion.span className="text-3xl font-bold font-mono text-neutral-900">{rounded}</motion.span>`.

**4 Stat Cards (REPT-01):**
- Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-8`.
- Card 1: "Paquetes Activos" -- `usePaquetes().filter(p => p.estado === "ACTIVO" && !p.deletedAt).length`, icon: `Package`, color: `#3BBFAD`.
- Card 2: "Aereos" -- `useAereos().filter(a => !a.deletedAt).length`, icon: `Plane`, color: `#8B5CF6`.
- Card 3: "Alojamientos" -- `useAlojamientos().filter(a => !a.deletedAt).length`, icon: `Hotel`, color: `#E8913A`.
- Card 4: "Visitas Web" -- hardcoded simulated value. Use `useBrand().activeBrandId` to pick: `brand-1` = 1247, `brand-2` = 892. Icon: `Eye`, color: `#2B8AFF`. Import `useBrand` from `@/components/providers/AuthProvider`.

**CRITICAL -- Brand-filtered junction data:**
All junction table data (paqueteAereos, paqueteAlojamientos) MUST be filtered through brand-safe paquete IDs. Compute once at top:
```typescript
const paquetes = usePaquetes(); // already brand-filtered
const paqueteIds = useMemo(() => new Set(paquetes.map(p => p.id)), [paquetes]);
```
Then filter all junction records: `state.paqueteAereos.filter(pa => paqueteIds.has(pa.paqueteId))` and same for `paqueteAlojamientos`. This prevents cross-brand data leaks (Pitfall 4 from RESEARCH.md).
  </action>
  <verify>TypeScript check deferred to Task 2 after full file is complete (Task 1 produces partial file that Task 2 finishes).</verify>
  <done>Reportes page renders 4 animated stat cards with correct data aggregation and brand-safe filtering.</done>
</task>

<task type="auto">
  <name>Task 2: Add recharts bar chart and hoteles table to Reportes page</name>
  <files>src/app/(admin)/reportes/page.tsx</files>
  <action>
Continue building the reportes page by adding the bar chart and hoteles table below the stat cards.

**Bar Chart -- Paquetes por Destino (REPT-02):**
- Derive `destinoChartData` with `useMemo`:
  1. Build `aereoMap: Record<string, string>` from `aereos` (aereoId -> destino).
  2. For each active paquete (`estado === "ACTIVO" && !deletedAt`), find its first paqueteAereo from the brand-filtered junction records, look up destino from aereoMap. Fallback to "Otro".
  3. Count occurrences per destino. Convert to array `{ destino: string, count: number }[]`, sort descending by count.
- Wrap chart in a `<Card variant="default">` with `<CardHeader>` showing "Paquetes por Destino" (text-base font-semibold text-neutral-800).
- Inside `<CardContent>`:
  ```tsx
  <ResponsiveContainer width="100%" height={280}>
    <BarChart data={destinoChartData} margin={{ top: 8, right: 16, left: -8, bottom: 0 }}>
      <CartesianGrid strokeDasharray="3 3" stroke="rgba(26,26,46,0.06)" vertical={false} />
      <XAxis
        dataKey="destino"
        tick={{ fill: "rgba(26,26,46,0.5)", fontSize: 12 }}
        axisLine={false}
        tickLine={false}
      />
      <YAxis
        tick={{ fill: "rgba(26,26,46,0.4)", fontSize: 11 }}
        axisLine={false}
        tickLine={false}
        allowDecimals={false}
      />
      <Tooltip
        contentStyle={{
          ...glassMaterials.frosted,
          border: "none",
          borderRadius: 10,
          padding: "8px 12px",
          fontSize: 12,
          color: "rgba(26,26,46,0.8)",
        }}
        cursor={{ fill: "rgba(59,191,173,0.06)" }}
      />
      <Bar dataKey="count" radius={[6, 6, 0, 0]} fill="rgba(59,191,173,0.75)" />
    </BarChart>
  </ResponsiveContainer>
  ```
- Import: `BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer` from `recharts`.
- Import `glassMaterials` from `@/components/lib/glass`.
- If `destinoChartData` is empty, show "Sin datos para grafico" placeholder.

**Hoteles Mas Usados Table (REPT-03):**
- Derive `hotelesData` with `useMemo`:
  1. Use brand-filtered paqueteAlojamientos (already computed in Task 1).
  2. Count occurrences of each `alojamientoId`.
  3. Join to `alojamientos` from `useAlojamientos()` to get `nombre`.
  4. Sort descending by count, take top 8.
  5. Result shape: `{ nombre: string; count: number }[]`.
- Render inside a `<Card variant="default">` with `<CardHeader>` showing "Hoteles Mas Usados".
- Use the glass `Table` component: `<Table>`, `<TableHeader>`, `<TableRow>`, `<TableHead>`, `<TableBody>`, `<TableCell>` from `@/components/ui/Table`.
- Columns: Hotel (nombre), Paquetes (count as number).
- If empty, show "Sin datos de hoteles" inside the card.

**Page layout:**
- `<PageHeader title="Reportes" subtitle="Metricas y reportes del sistema" />`
- Stat cards row (from Task 1).
- Two-column layout: left (2/3 width on lg) = bar chart card, right (1/3 width on lg) = hoteles table card.
- Use `flex flex-col lg:flex-row gap-6`.
- On mobile, stack vertically.

**Additional imports for this task:**
- `BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer` from `recharts`
- `Table, TableHeader, TableBody, TableRow, TableHead, TableCell` from `@/components/ui/Table`
- `glassMaterials` from `@/components/lib/glass`
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Run `npm run build` and verify /reportes route compiles successfully (recharts requires "use client" directive which is already present).</verify>
  <done>Reportes page shows a recharts BarChart with paquetes by destino data and a glass table of top 8 most-used hotels with usage counts. All data is brand-filtered through paquete IDs to prevent cross-brand leaks.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds (recharts client component check)
- 4 stat cards animate counters from 0 to target on mount
- Bar chart renders bars for each destino with correct counts
- Tooltip on bar chart hover uses glass styling
- Hoteles table shows hotel names sorted by usage count
- Switching brands shows different data (brand-filtered)
- "Visitas Web" card shows 1247 for brand-1, 892 for brand-2
</verification>

<success_criteria>
1. Four stat cards render with animated counters: Paquetes Activos, Aereos, Alojamientos, Visitas Web (simulated)
2. recharts BarChart renders paquetes grouped by destino with teal bars and glass-styled tooltip
3. Glass Table shows top 8 most-used hotels with name and paquete count
4. All junction data is brand-filtered through paquete IDs (no cross-brand data leaks)
5. Page uses glass design system consistently (Card, Table, glassMaterials for tooltip)
6. ResponsiveContainer uses fixed height (280) to avoid 0-width first render bug
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-notificaciones-reportes/08-03-SUMMARY.md`
</output>
