---
phase: 08-dashboard-notificaciones-reportes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/dashboard/page.tsx
  - src/app/(admin)/page.tsx
  - src/app/page.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/Topbar.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "Dashboard renders 4 liquid glass stat cards showing Paquetes Activos, Aereos, Alojamientos, and Proveedores counts with animated counter numbers"
    - "Dashboard shows a recent activity feed with the last 5 paquetes sorted by updatedAt"
    - "Dashboard shows quick access link cards for all modules visible to the current user role"
    - "Authenticated users landing on / are redirected to /dashboard"
  artifacts:
    - path: "src/app/(admin)/dashboard/page.tsx"
      provides: "Full dashboard page with stat cards, activity feed, and quick links"
      min_lines: 120
    - path: "src/app/(admin)/page.tsx"
      provides: "Redirect from / to /dashboard"
    - path: "src/app/page.tsx"
      provides: "Root redirect updated to /dashboard"
  key_links:
    - from: "src/app/(admin)/dashboard/page.tsx"
      to: "PackageProvider"
      via: "usePaquetes() for stat counts and activity feed"
      pattern: "usePaquetes\\(\\)"
    - from: "src/app/(admin)/dashboard/page.tsx"
      to: "ServiceProvider"
      via: "useAereos(), useAlojamientos() for stat counts"
      pattern: "useAereos\\(\\)|useAlojamientos\\(\\)"
    - from: "src/app/(admin)/dashboard/page.tsx"
      to: "motion/react"
      via: "animate(useMotionValue, target) for counter animation"
      pattern: "useMotionValue|animate"
    - from: "src/app/(admin)/dashboard/page.tsx"
      to: "AuthProvider"
      via: "useAuth().visibleModules for filtered quick links"
      pattern: "visibleModules"
---

<objective>
Build the Dashboard page with 4 animated stat cards, a recent activity feed, and quick access links to all modules. Also update root redirect from /paquetes to /dashboard.

Purpose: The dashboard is the landing page of the admin panel -- users see a data-rich overview immediately after login, with animated counters and quick navigation to all modules.
Output: Full dashboard page at /dashboard, updated root and admin root redirects.
</objective>

<execution_context>
@/Users/diegothx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diegothx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-dashboard-notificaciones-reportes/08-RESEARCH.md
@src/components/ui/Card.tsx
@src/components/lib/glass.ts
@src/components/lib/animations.ts
@src/components/providers/PackageProvider.tsx
@src/components/providers/ServiceProvider.tsx
@src/components/providers/AuthProvider.tsx
@src/components/layout/Sidebar.tsx
@src/components/layout/PageHeader.tsx
@src/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dashboard page with animated stat cards, activity feed, and quick links</name>
  <files>src/app/(admin)/dashboard/page.tsx</files>
  <action>
Create directory `src/app/(admin)/dashboard/` if it does not exist, then create `src/app/(admin)/dashboard/page.tsx` as a `"use client"` component with the full dashboard layout.

**Animated Stat Counter component (inline in this file):**
- Create a `StatCard` component that accepts `icon` (lucide-react component), `label` (string), `value` (number), and `color` (string for icon bg tint).
- Use `useMotionValue(0)` and `useTransform(count, Math.round)` from `motion/react` to create an animated counter.
- On mount, call `animate(count, value, { duration: 1.2, ease: "easeOut" })` and return `controls.stop` in cleanup.
- Render using `<motion.span>{rounded}</motion.span>` for the number display (NOT plain `<span>` -- MotionValue requires motion element).
- Wrap in `<Card variant="stat">` with `<CardContent>`. Use `<StatIcon>` for the icon container with dynamic background color via `style={{ background: \`\${color}20\`, color }}`.
- Text: label in `text-xs text-neutral-400 font-medium uppercase tracking-wide mb-1`, number in `text-3xl font-bold font-mono text-neutral-900`.

**4 Stat Cards section:**
- Row of 4 cards in `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-8`.
- Cards: (1) Paquetes Activos -- count from `usePaquetes().filter(p => p.estado === "ACTIVO" && !p.deletedAt).length`, icon: `Package`, color: `#3BBFAD`. (2) Aereos -- count from `useAereos().filter(a => !a.deletedAt).length`, icon: `Plane`, color: `#8B5CF6`. (3) Alojamientos -- count from `useAlojamientos().filter(a => !a.deletedAt).length`, icon: `Hotel`, color: `#E8913A`. (4) Proveedores -- count from `useProveedores().filter(p => !p.deletedAt).length`, icon: `Truck`, color: `#2B8AFF`.
- Import icons from `lucide-react`: `Package, Plane, Hotel, Truck`.
- Import `useProveedores` from `@/components/providers/CatalogProvider` (NOT ServiceProvider — useProveedores is exported from CatalogProvider).

**Recent Activity feed:**
- Wrap in a `<Card variant="default">` with a `<CardHeader>` showing "Actividad Reciente" in `text-base font-semibold text-neutral-800`.
- Get paquetes from `usePaquetes()`, filter out deleted, sort by `updatedAt` descending, take first 5.
- Render each as a row: paquete titulo (font-medium text-sm text-neutral-800), estado badge using `<Badge>` component with correct variant mapping (ACTIVO=active, BORRADOR=draft, PAUSADO=pending), and a relative time string (e.g., "Hace 2 dias") computed from `updatedAt`. Show `createdAt === updatedAt ? "Creado" : "Editado"` as a small label.
- If no paquetes, show "Sin actividad reciente" placeholder text.

**Quick Access Links:**
- Get `visibleModules` from `useAuth()`.
- Define a module map (array of objects): `{ id, label, icon, href, description }` for all 12 modules -- reuse the same data as the Sidebar navGroups but with added short descriptions. Only render modules where `visibleModules.includes(id)`.
- Render in a `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4` section with heading "Acceso Rapido" in `text-base font-semibold text-neutral-800 mb-3`.
- Each link: `<Link href={href}>` wrapping a `<Card variant="default" className="cursor-pointer">` with icon (24px, teal color), module label (font-medium text-sm), and description (text-xs text-neutral-400).
- Import icons from `lucide-react`: `LayoutDashboard, Package, Plane, Hotel, Bus, Ship, ShieldCheck, Truck, Users, Settings, Bell, BarChart3`. Import `Link` from `next/link`.

**Page structure:**
- `<PageHeader title="Dashboard" subtitle="Resumen general del sistema" />` at top.
- Stat cards grid.
- Two-column layout below stats: left column (2/3 width on lg) = activity feed, right column (1/3 width on lg) = quick access links. On mobile, stack vertically.
- Use `flex flex-col lg:flex-row gap-6` for the two-column layout.

**Imports needed:**
- `motion, useMotionValue, animate, useTransform` from `motion/react`
- `Card, CardContent, CardHeader, StatIcon` from `@/components/ui/Card`
- `Badge` from `@/components/ui/Badge`
- `PageHeader` from `@/components/layout/PageHeader`
- `usePaquetes` from `@/components/providers/PackageProvider`
- `useAereos, useAlojamientos` from `@/components/providers/ServiceProvider`
- `useProveedores` from `@/components/providers/CatalogProvider`
- `useAuth` from `@/components/providers/AuthProvider`
- `Link` from `next/link`
- All needed lucide-react icons
- `useEffect, useMemo` from `react`
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors in dashboard/page.tsx. Run `npm run build` or `npx next build` and verify /dashboard route compiles.</verify>
  <done>Dashboard page renders 4 animated stat cards with real data counts, a recent activity feed with last 5 paquetes, and role-filtered quick access links to all modules.</done>
</task>

<task type="auto">
  <name>Task 2: Update routing -- redirect root to /dashboard, update sidebar and topbar links</name>
  <files>src/app/(admin)/page.tsx, src/app/page.tsx, src/components/layout/Sidebar.tsx, src/components/layout/Topbar.tsx</files>
  <action>
**1. Update `src/app/(admin)/page.tsx`:**
Replace the DashboardPage stub with a redirect to `/dashboard`:
```tsx
"use client";
import { useEffect } from "react";
import { useRouter } from "next/navigation";
export default function AdminRootPage() {
  const router = useRouter();
  useEffect(() => { router.replace("/dashboard"); }, [router]);
  return null;
}
```

**2. Update `src/app/page.tsx`:**
Change `router.replace("/paquetes")` to `router.replace("/dashboard")`. Update the comment from `// Default landing page (Dashboard is Phase 8)` to `// Default landing page`.

**3. Update `src/components/layout/Sidebar.tsx`:**
Change the Dashboard nav item href from `"/"` to `"/dashboard"`:
```
{ id: "dashboard", label: "Dashboard", icon: LayoutDashboard, href: "/dashboard" },
```
Also update the `isActive` function: remove the special case `if (href === "/") return pathname === "/";` since dashboard now uses `/dashboard` like every other module.

**4. Update `src/components/layout/Topbar.tsx`:**
Change the breadcrumb root from `{ label: "Dashboard", href: "/" }` to `{ label: "Dashboard", href: "/dashboard" }`.
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Verify that the Sidebar, Topbar, root page, and admin root page all reference `/dashboard` consistently.</verify>
  <done>Navigating to `/` redirects to `/dashboard`. Sidebar dashboard link points to `/dashboard`. Breadcrumb root links to `/dashboard`. The old `/paquetes` default landing is replaced.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `/dashboard` route renders stat cards, activity feed, and quick links
- Root `/` redirects to `/dashboard`
- Sidebar dashboard link navigates to `/dashboard`
- Stat card counters animate from 0 to their target values on mount
- Activity feed shows last 5 paquetes sorted by updatedAt
- Quick links respect visibleModules from user role
</verification>

<success_criteria>
1. Dashboard displays 4 liquid glass stat cards with animated counter numbers showing real aggregated data
2. Recent activity feed shows the 5 most recently updated paquetes with status badges
3. Quick access links render for all modules visible to the current user role
4. Root redirect goes to /dashboard instead of /paquetes
5. All components use the glass design system (Card variant="stat", glassMaterials, motion animations)
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-notificaciones-reportes/08-01-SUMMARY.md`
</output>
