---
phase: 04-paquetes-module
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/paquetes/nuevo/page.tsx
  - src/app/(admin)/paquetes/[slug]/page.tsx
  - src/app/(admin)/paquetes/[slug]/_components/DatosTab.tsx
autonomous: true
requirements: [PAQT-03, PAQT-04, PAQT-09, PAQT-13, PAQT-14]

must_haves:
  truths:
    - "User can navigate to /paquetes/{id} and see a 5-tab layout with Datos selected by default"
    - "User can click each tab (Datos, Servicios, Precios, Fotos, Publicacion) and the URL updates to ?tab={name}"
    - "User can use browser back/forward and the active tab stays in sync with the URL"
    - "The Datos tab shows a form with all Paquete fields -- titulo, descripcion, noches, salidas, temporada, tipo, textoVisual"
    - "User can fill out the form at /paquetes/nuevo and create a new paquete that appears in the list"
    - "Editing fields in the Datos tab persists changes via updatePaquete"
  artifacts:
    - path: "src/app/(admin)/paquetes/[slug]/page.tsx"
      provides: "Detail page shell with 5-tab layout and URL sync"
      min_lines: 60
    - path: "src/app/(admin)/paquetes/[slug]/_components/DatosTab.tsx"
      provides: "Datos tab form with all Paquete fields"
      min_lines: 80
    - path: "src/app/(admin)/paquetes/nuevo/page.tsx"
      provides: "Create new paquete page with full form"
      min_lines: 60
  key_links:
    - from: "src/app/(admin)/paquetes/[slug]/page.tsx"
      to: "PackageProvider"
      via: "usePaquetes() to find paquete by slug (which is the paquete.id)"
      pattern: "usePaquetes|paquete\\.id"
    - from: "src/app/(admin)/paquetes/[slug]/page.tsx"
      to: "URL search params"
      via: "useSearchParams for tab sync"
      pattern: "useSearchParams|tab="
    - from: "src/app/(admin)/paquetes/nuevo/page.tsx"
      to: "PackageProvider"
      via: "usePackageActions().createPaquete"
      pattern: "createPaquete"
    - from: "src/app/(admin)/paquetes/[slug]/_components/DatosTab.tsx"
      to: "PackageProvider"
      via: "usePackageActions().updatePaquete"
      pattern: "updatePaquete"
---

<objective>
Build the paquete detail page with 5-tab shell, URL-synced tab navigation, the Datos tab form with all entity fields, and the /paquetes/nuevo create page.

Purpose: The detail page is the hub for all paquete editing. The Datos tab and create page are the foundation for the paquete data model.
Output: Three new files -- detail shell, Datos tab component, and create page.
</objective>

<execution_context>
@/Users/diegothx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diegothx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-paquetes-module/04-RESEARCH.md
@.planning/phases/03-data-layer-types/03-05-SUMMARY.md
@src/components/providers/PackageProvider.tsx
@src/components/providers/CatalogProvider.tsx
@src/components/providers/AuthProvider.tsx
@src/components/ui/Tabs.tsx
@src/components/ui/Input.tsx
@src/components/ui/Select.tsx
@src/components/ui/Button.tsx
@src/components/layout/PageHeader.tsx
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detail page shell with 5-tab layout and URL sync</name>
  <files>src/app/(admin)/paquetes/[slug]/page.tsx</files>
  <action>
Create the dynamic route detail page. The `slug` parameter is the paquete's `id` (not a slugified title -- per research recommendation to avoid collision issues).

1. **File:** `src/app/(admin)/paquetes/[slug]/page.tsx` -- mark `"use client"`.

2. **Imports:** `useParams`, `useRouter`, `useSearchParams` from `next/navigation`. `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` from `@/components/ui/Tabs`. `PageHeader` from `@/components/layout/PageHeader`. `Button` from `@/components/ui/Button`. `Badge` from `@/components/ui/Badge`. `usePaquetes` from PackageProvider. `useAuth` from AuthProvider. `ArrowLeft` from `lucide-react`. Tab content components (DatosTab, then placeholder divs for Servicios/Precios/Fotos/Publicacion that will be replaced in plans 03-05).

3. **Tab constants:** `const TABS = ['datos', 'servicios', 'precios', 'fotos', 'publicacion'] as const;` and Spanish label map: `{ datos: 'Datos', servicios: 'Servicios', precios: 'Precios', fotos: 'Fotos', publicacion: 'Publicacion' }`.

4. **Component logic:**
   - Read `slug` from `useParams<{ slug: string }>()`.
   - Find paquete: `usePaquetes().find(p => p.id === slug)`. If not found, render a "Paquete no encontrado" message with a back link.
   - Read active tab from `useSearchParams().get('tab') ?? 'datos'`.
   - Tab change handler: `router.replace(\`/paquetes/${slug}?tab=${tab}\`, { scroll: false })`.

5. **Render:**
   - `PageHeader` with title = paquete.titulo, subtitle including estado badge inline, action = back button `<Button variant="ghost" onClick={() => router.push('/paquetes')}><ArrowLeft /> Volver a Paquetes</Button>`.
   - `Tabs` component with `value={activeTab}` and `onValueChange={handleTabChange}`. The outer Tabs uses default `layoutId="activeTab"`.
   - 5 `TabsTrigger` entries with Spanish labels.
   - 5 `TabsContent` entries:
     - `datos`: `<DatosTab paquete={paquete} />` (built in Task 2)
     - `servicios`: Placeholder `<div className="p-8 text-center text-neutral-500">Servicios tab -- coming in Plan 04-03</div>`
     - `precios`: Placeholder `<div>Precios tab -- coming in Plan 04-04</div>`
     - `fotos`: Placeholder `<div>Fotos tab -- coming in Plan 04-04</div>`
     - `publicacion`: Placeholder `<div>Publicacion tab -- coming in Plan 04-05</div>`

6. **Not-found state:** If paquete is null, render a Card with "Paquete no encontrado" and a "Volver" button. Use glass Card component.

7. **All text in Spanish.**
  </action>
  <verify>
Navigate to `/paquetes/{any-paquete-id}` -- the 5-tab layout renders. Clicking tabs updates the URL `?tab=` param. Refresh on `?tab=precios` keeps Precios active. Back button returns to /paquetes.
  </verify>
  <done>
Detail page renders 5 tabs synced to URL search params. Tab switching animates the gradient underline. Non-existent ID shows "Paquete no encontrado".
  </done>
</task>

<task type="auto">
  <name>Task 2: Datos tab form and /paquetes/nuevo create page</name>
  <files>src/app/(admin)/paquetes/[slug]/_components/DatosTab.tsx, src/app/(admin)/paquetes/nuevo/page.tsx</files>
  <action>
**DatosTab.tsx** -- the edit form for existing paquetes:

1. **File:** `src/app/(admin)/paquetes/[slug]/_components/DatosTab.tsx` -- mark `"use client"`.

2. **Props:** `{ paquete: Paquete }` -- receives the current paquete from the detail page.

3. **Imports:** `useState` from React. `Input` from `@/components/ui/Input`. `Select` from `@/components/ui/Select`. `Button` from `@/components/ui/Button`. `Card` from `@/components/ui/Card`. `usePackageActions` from PackageProvider. `useTemporadas`, `useTiposPaquete` from CatalogProvider. `useAuth` from AuthProvider. `useToast` from Toast. `Paquete` from types.

4. **Local form state:** Initialize from `paquete` prop:
   - `titulo: string`, `descripcion: string`, `textoVisual: string`, `noches: number`, `salidas: string`, `temporadaId: string`, `tipoPaqueteId: string`.
   - Use `useState` with initial values from the paquete prop.

5. **Save handler:** Call `updatePaquete({ ...paquete, titulo, descripcion, textoVisual, noches, salidas, temporadaId, tipoPaqueteId, updatedAt: new Date().toISOString() })`. Show success toast: "Paquete actualizado".

6. **Form layout:** Use a glass `Card` as the form container. Two-column grid layout (`grid grid-cols-1 md:grid-cols-2 gap-4 p-6`):
   - Row 1: Titulo (full width, `col-span-2`)
   - Row 2: Noches (number input), Salidas (text input for departure dates/frequency)
   - Row 3: Temporada (Select dropdown from `useTemporadas()`), Tipo de Paquete (Select from `useTiposPaquete()`)
   - Row 4: Descripcion (full width textarea -- use Input or a simple `<textarea>` styled with glass pattern. Since there is no Textarea component, use a `<textarea>` element with glass input styling: `className="w-full rounded-[10px] border border-neutral-150/50 bg-neutral-900/30 px-3 py-2 text-sm text-neutral-100 placeholder:text-neutral-500 focus:outline-none focus:ring-2 focus:ring-violet-500/50 backdrop-blur-sm"` and minimum 3 rows.)
   - Row 5: Texto Visual (full width textarea, 2 rows)
   - Save button at the bottom right: `<Button onClick={handleSave}>Guardar Cambios</Button>`. Only show if `canEdit` from `useAuth()`.

7. **Disabled state for VENDEDOR:** If `!canEdit`, all inputs should have `disabled` or `readOnly` prop. Do NOT show the save button.

8. **All labels in Spanish:** "Titulo", "Noches", "Salidas", "Temporada", "Tipo de Paquete", "Descripcion", "Texto Visual", "Guardar Cambios".

---

**nuevo/page.tsx** -- the create page:

1. **File:** `src/app/(admin)/paquetes/nuevo/page.tsx` -- mark `"use client"`. This MUST be a static sibling directory to `[slug]`, not inside it (critical for Next.js App Router route matching).

2. **Structure:** Similar form to DatosTab but initialized with empty/default values:
   - titulo: `""`, descripcion: `""`, textoVisual: `""`, noches: `7`, salidas: `"Consultar"`, temporadaId: first available temporada, tipoPaqueteId: first available tipo.

3. **Create handler:** Call `createPaquete({...formData, brandId: activeBrandId, estado: 'BORRADOR', destacado: false, netoCalculado: 0, markup: 30, precioVenta: 0, moneda: 'USD', validezDesde: new Date().toISOString(), validezHasta: new Date(Date.now() + 365*24*60*60*1000).toISOString(), ordenServicios: []})`. Get `activeBrandId` from `useBrand()`. After creation, `router.push(\`/paquetes/${newPaquete.id}?tab=datos\`)` to navigate to the new paquete's detail page. Show success toast.

4. **Render:** `PageHeader` with title "Nuevo Paquete", back button action. Same two-column form grid as DatosTab but with a "Crear Paquete" button instead of "Guardar Cambios".

5. **VENDEDOR guard:** If `!canEdit`, redirect to `/paquetes` immediately via `useEffect`.

6. **All text in Spanish.**
  </action>
  <verify>
Navigate to `/paquetes/nuevo` -- the create form renders with empty fields. Fill in titulo, select temporada and tipo, click "Crear Paquete" -- redirected to the new paquete's detail page. Navigate to an existing paquete, change titulo in Datos tab, click "Guardar Cambios" -- title updates. TypeScript compiles without errors.
  </verify>
  <done>
DatosTab form renders all fields and persists changes via updatePaquete. /paquetes/nuevo creates a new paquete in BORRADOR estado and navigates to its detail page. VENDEDOR sees read-only form with no save button and is redirected from /nuevo.
  </done>
</task>

</tasks>

<verification>
- /paquetes/{id} renders 5 tabs with animated gradient underline
- Tab URL sync works: clicking tabs updates ?tab=, refresh preserves tab, back/forward works
- DatosTab displays all Paquete fields with correct values from state
- Editing and saving updates the paquete via updatePaquete
- /paquetes/nuevo renders create form, creates paquete in BORRADOR, redirects to detail
- /paquetes/nuevo is NOT captured by the [slug] dynamic route
- VENDEDOR sees read-only fields, no save button, gets redirected from /nuevo
</verification>

<success_criteria>
The paquete detail page shell exists with working tab navigation and URL sync. The Datos tab provides a complete edit form. The /nuevo page creates new paquetes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-paquetes-module/04-02-SUMMARY.md`
</output>
