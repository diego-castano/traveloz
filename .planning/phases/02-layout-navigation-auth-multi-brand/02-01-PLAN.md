---
phase: 02-layout-navigation-auth-multi-brand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth.ts
  - src/lib/brands.ts
  - src/components/providers/AuthProvider.tsx
  - src/components/providers/BrandProvider.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [AUTH-02, AUTH-03, AUTH-04, AUTH-05, BRND-02, BRND-03]

must_haves:
  truths:
    - "AuthProvider makes user, role, and permission state available to all components via useAuth()"
    - "BrandProvider makes active brand and brand-specific tokens available via useBrand()"
    - "Role config correctly restricts VENDEDOR to paquetes-only visible modules and read-only permissions"
    - "Switching brand via switchBrand() updates active brand tokens throughout the app"
    - "Root layout wraps entire app with AuthProvider > BrandProvider > ToastProvider"
  artifacts:
    - path: "src/lib/auth.ts"
      provides: "Role type, AuthUser interface, roleConfig, permission helpers"
      contains: "roleConfig"
    - path: "src/lib/brands.ts"
      provides: "BrandTokens interface, brandTokens data for TravelOz and DestinoIcono"
      contains: "brandTokens"
    - path: "src/components/providers/AuthProvider.tsx"
      provides: "AuthProvider component and useAuth() hook"
      exports: ["AuthProvider", "useAuth"]
    - path: "src/components/providers/BrandProvider.tsx"
      provides: "BrandProvider component and useBrand() hook"
      exports: ["BrandProvider", "useBrand"]
    - path: "src/app/layout.tsx"
      provides: "Root layout with provider composition"
      contains: "AuthProvider"
  key_links:
    - from: "src/components/providers/AuthProvider.tsx"
      to: "src/lib/auth.ts"
      via: "imports Role, AuthUser, roleConfig"
      pattern: "import.*from.*@/lib/auth"
    - from: "src/components/providers/BrandProvider.tsx"
      to: "src/lib/brands.ts"
      via: "imports BrandTokens, brandTokens"
      pattern: "import.*from.*@/lib/brands"
    - from: "src/app/layout.tsx"
      to: "src/components/providers/AuthProvider.tsx"
      via: "wraps children with AuthProvider"
      pattern: "<AuthProvider>"
---

<objective>
Create the AuthProvider and BrandProvider contexts that power all Phase 2 components -- role-based permissions, brand-specific tokens, and simulated login state.

Purpose: Every layout component (Sidebar, Topbar, Login, Admin Layout) needs auth state and brand tokens. These providers must exist first.
Output: AuthProvider.tsx, BrandProvider.tsx, auth.ts, brands.ts, updated root layout.tsx
</objective>

<execution_context>
@/Users/diegothx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diegothx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-navigation-auth-multi-brand/02-RESEARCH.md
@docs/design.json
@src/components/lib/glass.ts
@src/components/lib/animations.ts
@src/components/ui/Toast.tsx
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth and Brand type helpers + provider contexts</name>
  <files>src/lib/auth.ts, src/lib/brands.ts, src/components/providers/AuthProvider.tsx, src/components/providers/BrandProvider.tsx</files>
  <action>
Create `src/lib/auth.ts`:
- Export `type Role = "ADMIN" | "VENDEDOR" | "MARKETING"`
- Export `interface AuthUser { id: string; name: string; email: string; role: Role; brandId: string; }`
- Export `roleConfig` object keyed by Role with these fields per role:
  - `canEdit: boolean` (true for ADMIN, false for VENDEDOR/MARKETING)
  - `canSeePricing: { neto: boolean; markup: boolean; venta: boolean }` (all true for ADMIN; neto/markup false, venta true for VENDEDOR/MARKETING)
  - `visibleModules: string[]` (ADMIN gets all 12 modules: dashboard, paquetes, aereos, alojamientos, traslados, circuitos, seguros, proveedores, catalogos, perfiles, notificaciones, reportes; VENDEDOR gets ["paquetes"] only; MARKETING gets ["paquetes", "reportes"])
- Export hardcoded `DEMO_USERS` array with 5 users from PROMPT_CLAUDE_CODE.md:
  - user-1: Geronimo Cassoni, geronimo@traveloz.com.uy, ADMIN, brand-1
  - user-2: Santiago Rodriguez, santiago@traveloz.com.uy, ADMIN, brand-1
  - user-3: Equipo Ventas TravelOz, ventas@traveloz.com.uy, VENDEDOR, brand-1
  - user-4: Admin DestinoIcono, admin@destinoicono.com, ADMIN, brand-2
  - user-5: Equipo Ventas DestinoIcono, ventas@destinoicono.com, VENDEDOR, brand-2
- All demo users use password "admin" for simulated login

Create `src/lib/brands.ts`:
- Export `interface BrandTokens` with fields: id, name, slug, tagline, logo, logoWhite, sidebarGradient, sidebarBlur, sidebarGlow, sidebarTopGlow, loginBackground, accentPrimary, accentSecondary, brandGlow
- Export `brandTokens: Record<string, BrandTokens>` with EXACT values from design.json brands section:
  - "brand-1" (TravelOz): sidebarGradient "linear-gradient(180deg, rgba(108,43,217,0.95) 0%, rgba(68,20,150,0.97) 40%, rgba(15,8,30,0.99) 100%)", etc.
  - "brand-2" (DestinoIcono): sidebarGradient "linear-gradient(180deg, rgba(26,58,92,0.95) 0%, rgba(15,36,64,0.97) 40%, rgba(6,16,32,0.99) 100%)", etc.
- Export `BRAND_LIST` as array of BrandTokens for dropdown iteration

Create `src/components/providers/AuthProvider.tsx`:
- "use client" directive at top
- Import Role, AuthUser, roleConfig, DEMO_USERS from @/lib/auth
- Create AuthContext with createContext<AuthContextValue | null>(null)
- AuthContextValue interface:
  - user: AuthUser | null
  - isAuthenticated: boolean
  - isAdmin: boolean
  - isVendedor: boolean
  - canEdit: boolean
  - canSeePricing: { neto: boolean; markup: boolean; venta: boolean }
  - visibleModules: string[]
  - login: (email: string, password: string) => boolean
  - logout: () => void
- `login` function: find user in DEMO_USERS by email (password always "admin"), set user state, return true/false
- `logout` function: set user to null
- Derive isAdmin, isVendedor, canEdit, canSeePricing, visibleModules from roleConfig[user.role]
- Memoize the context value with useMemo to prevent unnecessary re-renders (per research pitfall 3)
- Export `useAuth()` hook that throws if used outside AuthProvider

Create `src/components/providers/BrandProvider.tsx`:
- "use client" directive at top
- Import BrandTokens, brandTokens, BRAND_LIST from @/lib/brands
- Create BrandContext with createContext<BrandContextValue | null>(null)
- BrandContextValue interface:
  - activeBrandId: string
  - activeBrand: BrandTokens
  - brands: BrandTokens[]
  - switchBrand: (brandId: string) => void
- Default activeBrandId: "brand-1" (TravelOz)
- switchBrand updates the activeBrandId state
- Memoize context value with useMemo
- Export `useBrand()` hook that throws if used outside BrandProvider

CRITICAL decisions to follow:
- Glass materials use inline style objects (not Tailwind) -- this is established pattern from Phase 1
- motion (NOT framer-motion): import { motion } from 'motion/react'
- WebkitBackdropFilter included for Safari -- follow glass.ts pattern
- All design token values from docs/design.json EXACTLY, no approximations
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Verify all 4 files exist with correct exports:
- `grep -c "export" src/lib/auth.ts` should show multiple exports
- `grep -c "export" src/lib/brands.ts` should show multiple exports
- `grep "useAuth\|AuthProvider" src/components/providers/AuthProvider.tsx` confirms both exports
- `grep "useBrand\|BrandProvider" src/components/providers/BrandProvider.tsx` confirms both exports
  </verify>
  <done>auth.ts exports Role, AuthUser, roleConfig, DEMO_USERS. brands.ts exports BrandTokens, brandTokens, BRAND_LIST. AuthProvider provides useAuth() with all role-derived booleans. BrandProvider provides useBrand() with brand-specific tokens. No type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update root layout with provider composition</name>
  <files>src/app/layout.tsx</files>
  <action>
Update `src/app/layout.tsx` to compose providers around the entire app.

The root layout must:
1. Add "use client" directive (needed for providers that use hooks)
   WAIT -- actually the root layout should remain a server component for metadata export. Instead, create a `src/components/providers/Providers.tsx` client wrapper component that composes all providers, and import it in the root layout.

Revised approach:
- Create `src/components/providers/Providers.tsx` ("use client"):
  - Import AuthProvider from ./AuthProvider
  - Import BrandProvider from ./BrandProvider
  - Import ToastProvider from @/components/ui/Toast
  - Compose: AuthProvider > BrandProvider > ToastProvider > {children}
  - Export as default

- Update `src/app/layout.tsx` (keep as server component):
  - Import Providers from @/components/providers/Providers
  - Import DM_Sans and Playfair_Display from 'next/font/google'
  - Configure DM Sans (weights: 400,500,600,700, variable: '--font-body')
  - Configure Playfair Display (weights: 400,700, variable: '--font-display')
  - Apply font variables to html element className
  - Add antialiased to body className
  - Wrap {children} with Providers component
  - Keep existing metadata export

Font setup is needed because design.json specifies 'DM Sans' for body and 'Playfair Display' for display typography, and the existing layout does not load them yet.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Run `npm run dev` and check the dev server starts without errors. Open browser devtools and confirm:
- HTML element has font CSS variables
- No React context errors in console
  </verify>
  <done>Root layout loads DM Sans and Playfair Display fonts, wraps all pages with AuthProvider > BrandProvider > ToastProvider via the Providers client component. Dev server runs without errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run dev` starts without errors
- AuthProvider, BrandProvider, and ToastProvider are composed at root level
- DM Sans and Playfair Display fonts are loaded via next/font/google
</verification>

<success_criteria>
- useAuth() returns correct role config for ADMIN (canEdit: true, all pricing visible, all modules)
- useAuth() returns correct role config for VENDEDOR (canEdit: false, only venta visible, only paquetes)
- useBrand() returns TravelOz tokens by default with correct sidebarGradient
- useBrand().switchBrand("brand-2") switches to DestinoIcono tokens
- Root layout composes all providers and loads required fonts
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-navigation-auth-multi-brand/02-01-SUMMARY.md`
</output>
