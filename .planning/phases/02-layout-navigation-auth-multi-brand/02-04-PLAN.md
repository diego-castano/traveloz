---
phase: 02-layout-navigation-auth-multi-brand
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/login/page.tsx
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "Login page renders with brand-specific mesh gradient background and noise overlay"
    - "Login card uses liquid glass material with scale+y+blur entrance animation"
    - "Login page has email and password inputs with 'Bienvenido' title in Playfair Display 28px"
    - "Submitting credentials calls auth.login() and navigates to admin on success"
    - "Login page exists OUTSIDE the (admin) route group at /login"
  artifacts:
    - path: "src/app/login/page.tsx"
      provides: "Full login page with mesh gradient, noise, liquid glass card"
      exports: ["default"]
      min_lines: 80
  key_links:
    - from: "src/app/login/page.tsx"
      to: "src/components/providers/AuthProvider.tsx"
      via: "useAuth() for login function"
      pattern: "useAuth\\(\\)"
    - from: "src/app/login/page.tsx"
      to: "src/components/providers/BrandProvider.tsx"
      via: "useBrand() for brand-specific login background"
      pattern: "useBrand\\(\\)"
    - from: "src/app/login/page.tsx"
      to: "src/components/lib/glass.ts"
      via: "glassMaterials.liquid for card styling"
      pattern: "glassMaterials"
---

<objective>
Build the login page with animated mesh gradient background, noise overlay, and liquid glass card for simulated authentication.

Purpose: Entry point for the admin panel. Sets user role and brand in state. Demonstrates the premium glassmorphism visual on the very first screen the user sees.
Output: src/app/login/page.tsx
</objective>

<execution_context>
@/Users/diegothx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diegothx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-navigation-auth-multi-brand/02-RESEARCH.md
@.planning/phases/02-layout-navigation-auth-multi-brand/02-01-SUMMARY.md
@docs/design.json
@src/components/lib/glass.ts
@src/components/lib/animations.ts
@src/components/ui/Input.tsx
@src/components/ui/Button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login page with mesh gradient, noise overlay, and liquid glass card</name>
  <files>src/app/login/page.tsx</files>
  <action>
Create `src/app/login/page.tsx` ("use client"):

This page lives OUTSIDE the (admin) route group, so it does NOT get the admin layout (no sidebar, no topbar). This is critical (research pitfall 6).

**Imports:**
- `{ useState } from "react"`
- `{ useRouter } from "next/navigation"`
- `{ motion } from "motion/react"` (NOT framer-motion)
- `{ useAuth } from "@/components/providers/AuthProvider"`
- `{ useBrand } from "@/components/providers/BrandProvider"`
- `{ glassMaterials } from "@/components/lib/glass"`
- `{ Input } from "@/components/ui/Input"`
- `{ Button } from "@/components/ui/Button"`

**State:**
- email: string (default "")
- password: string (default "")
- error: string (default "")
- loading: boolean (default false)

**Login handler:**
```
const handleLogin = (e: FormEvent) => {
  e.preventDefault();
  setError("");
  const success = auth.login(email, password);
  if (success) {
    router.push("/");
  } else {
    setError("Email o contraseña incorrectos");
  }
};
```

**Page structure:**

1. **Full-screen wrapper:** `className="relative flex min-h-screen items-center justify-center overflow-hidden"`

2. **Mesh gradient background:** Absolutely positioned div covering viewport:
   - style: { background: activeBrand.loginBackground } (brand-specific from BrandProvider)
   - className: "absolute inset-0 animate-mesh-float" (meshFloat keyframe already in tailwind.config.ts)

3. **Noise overlay:** Same pattern as AdminBackground:
   - className: "absolute inset-0 pointer-events-none"
   - style: opacity 0.025, mixBlendMode "overlay", SVG fractalNoise background from design.json glass.noise.backgroundSvg

4. **Login card:** motion.div with liquid glass material:
   - Entrance animation from design.json patterns.loginPage.card.entranceAnimation:
     - initial: { opacity: 0, y: 40, scale: 0.9, filter: "blur(10px)" }
     - animate: { opacity: 1, y: 0, scale: 1, filter: "blur(0px)" }
     - transition: { type: "spring", stiffness: 200, damping: 22, delay: 0.2 }
   - Style (inline -- locked decision):
     - ...glassMaterials.liquid (spread liquid material)
     - width: 420px (from design.json patterns.loginPage.card.width)
     - padding: 40px (from design.json)
     - borderRadius: 24px (from design.json)
     - boxShadow: design.json elevation-32 = "0 32px 64px -12px rgba(26,26,46,0.15), 0 12px 24px -8px rgba(26,26,46,0.08)"
   - className: "relative z-10"
   - Add the top accent line (::before equivalent via child div): "absolute inset-x-0 top-0 h-[2px] rounded-t-[24px]" with background: "linear-gradient(90deg, transparent, rgba(139,92,246,0.2), rgba(59,191,173,0.15), transparent)"

5. **Card content:**
   - Brand logo area: Show activeBrand.name as styled text or placeholder (since actual logo images may not exist yet)
   - Title: "Bienvenido" in `font-display text-[28px] font-bold text-neutral-900` (from design.json patterns.loginPage.title)
   - Subtitle: "Ingresa tus credenciales para continuar" in `text-sm text-neutral-400 mt-1`
   - Spacer: mt-8
   - Form with onSubmit={handleLogin}:
     - Email Input: label="Email", type="email", placeholder="tu@email.com", value={email}, onChange
     - Password Input: label="Contraseña", type="password", placeholder="••••••••", value={password}, onChange
     - Error display (if error): red text with error message, text-sm text-brand-red-500 mt-2
     - Submit Button: variant="primary", size="lg", className="w-full mt-6", children="Ingresar"
   - Demo credentials hint: small muted text below form showing available demo accounts:
     - "Demo: geronimo@traveloz.com.uy / admin" in text-xs text-neutral-300 mt-4 text-center

**Redirect check:** If user is already authenticated, redirect to "/" immediately:
```
const { isAuthenticated } = useAuth();
const router = useRouter();
if (isAuthenticated) {
  router.push("/");
  return null;
}
```
  </action>
  <verify>
Run `npx tsc --noEmit`. Verify:
- File exists at src/app/login/page.tsx (NOT inside (admin) directory)
- Uses useAuth() for login function
- Uses useBrand() for brand-specific loginBackground
- Card entrance animation matches design.json (scale 0.9, y 40, blur 10px, spring stiffness 200, damping 22)
- Liquid glass material applied to card
- "Bienvenido" title in font-display at 28px
  </verify>
  <done>Login page renders with brand-specific mesh gradient background, SVG noise overlay, liquid glass card with spring entrance animation, "Bienvenido" title in Playfair Display 28px, email/password inputs, and "Ingresar" button that calls auth.login() and navigates to admin on success.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Login page at /login (outside admin layout -- no sidebar/topbar visible)
- Mesh gradient uses brand-specific loginBackground from useBrand()
- Card uses glassMaterials.liquid with elevation-32 shadow
- Entrance animation: scale(0.9) + y(40) + blur(10px) -> normal with spring
</verification>

<success_criteria>
- Navigating to /login shows full-screen mesh gradient with noise overlay
- Liquid glass card animates in with spring entrance
- Entering geronimo@traveloz.com.uy / admin logs in as ADMIN and redirects to /
- Entering wrong credentials shows error message
- Already-authenticated users are redirected away from /login
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-navigation-auth-multi-brand/02-04-SUMMARY.md`
</output>
