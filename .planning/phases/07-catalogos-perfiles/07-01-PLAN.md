---
phase: 07-catalogos-perfiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/catalogos/page.tsx
autonomous: true
requirements:
  - CATL-01
  - CATL-02
  - CATL-03
  - CATL-04
  - CATL-05
  - CATL-06

must_haves:
  truths:
    - "Catalogos page renders with 5 tabs: Temporadas, Tipos de Paquete, Etiquetas, Paises y Ciudades, Regimenes"
    - "Switching tabs shows the corresponding catalog list with animated gradient indicator"
    - "User can create, edit, and delete Temporadas via modal with nombre, orden, activa fields"
    - "User can create, edit, and delete Tipos de Paquete via modal with nombre, orden, activo fields"
    - "User can create, edit, and delete Etiquetas via modal with nombre, slug, color fields; color swatch visible in table"
    - "User can create, edit, and delete Paises via modal; expanding a Pais row reveals inline Ciudades CRUD"
    - "User can create, edit, and delete Regimenes via modal with nombre, abrev fields"
  artifacts:
    - path: "src/app/(admin)/catalogos/page.tsx"
      provides: "Tabbed catalog management page with 5 tab sub-components"
      min_lines: 400
  key_links:
    - from: "src/app/(admin)/catalogos/page.tsx"
      to: "CatalogProvider"
      via: "useTemporadas, useTiposPaquete, useEtiquetas, usePaises, useRegimenes, useCatalogActions hooks"
      pattern: "use(Temporadas|TiposPaquete|Etiquetas|Paises|Regimenes|CatalogActions)"
    - from: "src/app/(admin)/catalogos/page.tsx"
      to: "@/components/ui/Tabs"
      via: "Tabs compound component with layoutId"
      pattern: "layoutId.*catalogos-tabs"
---

<objective>
Build the complete Catalogos page with a tabbed interface housing 5 independent catalog mini-managers (Temporadas, Tipos de Paquete, Etiquetas, Paises y Ciudades, Regimenes). Each tab provides full CRUD via modal dialogs, following the exact Seguros/Proveedores modal CRUD pattern from Phase 6.

Purpose: Replace the stub Catalogos page with a fully functional reference data management interface, consuming all the catalog CRUD actions that have existed since Phase 3 but had no UI.

Output: Single-file page component with 5 named tab function components.
</objective>

<execution_context>
@/Users/diegothx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diegothx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-catalogos-perfiles/07-RESEARCH.md

@src/app/(admin)/seguros/page.tsx
@src/app/(admin)/proveedores/page.tsx
@src/components/ui/Tabs.tsx
@src/components/providers/CatalogProvider.tsx
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalogos tabbed page shell + Temporadas, TiposPaquete, and Regimenes tabs</name>
  <files>src/app/(admin)/catalogos/page.tsx</files>
  <action>
Replace the existing stub Catalogos page with a full tabbed interface. Structure the file as:

1. **CatalogosPage default export** — renders `<PageHeader>` + `<Tabs defaultValue="temporadas" layoutId="catalogos-tabs">` with 5 TabsTrigger items and 5 TabsContent blocks, each delegating to a named function component.

2. **TemporadasTab function component** — follows the Proveedores page.tsx pattern EXACTLY:
   - Hooks: `useAuth()` for canEdit, `useBrand()` for activeBrandId, `useToast()`, `useTemporadas()`, `useCatalogActions()` for createTemporada/updateTemporada/deleteTemporada
   - Local state: modalOpen, editTarget (Temporada | null), deleteTarget (Temporada | null), isShaking, form ({nombre, orden, activa}), search, page
   - Filtered + paginated data via useMemo; page reset on search via useEffect
   - Handlers: handleOpenCreate (form defaults: nombre='', orden=temporadas.length+1, activa=true), handleOpenEdit, handleSave (create uses brandId: activeBrandId), handleOpenDelete, handleConfirmDelete with shake animation
   - Table columns: Nombre, Orden, Estado (Badge: activa ? variant="active" label="Activa" : variant="draft" label="Inactiva"), Acciones (Eye + Pencil + Trash2, gated by canEdit)
   - NO clone button (catalog items are simple, no need)
   - Create/Edit Modal: Input for nombre, Input type="number" for orden, Select for activa (options: [{value:"true",label:"Activa"},{value:"false",label:"Inactiva"}]) — convert string to boolean on save
   - Delete Modal with motion.div shake (spread deleteShake.animate.x into mutable array)
   - SearchFilter with empty filters array, placeholder "Buscar temporada..."
   - Empty state icon: Calendar from lucide-react

3. **TiposPaqueteTab function component** — same pattern as TemporadasTab:
   - Hooks: useTiposPaquete(), useCatalogActions() for createTipoPaquete/updateTipoPaquete/deleteTipoPaquete
   - Form: {nombre, orden, activo} — note: field is "activo" not "activa"
   - Table columns: Nombre, Orden, Estado (activo badge), Acciones
   - Empty state icon: Layers from lucide-react
   - SearchFilter placeholder: "Buscar tipo de paquete..."

4. **RegimenesTab function component** — same pattern:
   - Hooks: useRegimenes(), useCatalogActions() for createRegimen/updateRegimen/deleteRegimen
   - Form: {nombre, abrev}
   - Table columns: Nombre, Abreviatura (font-mono text-xs text-neutral-400), Acciones
   - No orden or activo fields for Regimen
   - Empty state icon: Utensils from lucide-react
   - SearchFilter placeholder: "Buscar regimen..."

**Anti-patterns to avoid (from RESEARCH.md):**
- Do NOT use useEffect to sync form state from editTarget — initialize form at setModalOpen(true) call site
- Do NOT lift tab modal state to page level — each tab manages its own state
- Pass layoutId="catalogos-tabs" to Tabs to avoid layoutId conflicts
- Use inline function components defined ABOVE the default export (not separate files)

**Imports:** PageHeader, Button, SearchFilter, Input, Select, Badge, Table/TableHeader/TableBody/TableRow/TableHead/TableCell, Modal/ModalHeader/ModalBody/ModalFooter, Pagination, Tabs/TabsList/TabsTrigger/TabsContent, motion from motion/react, interactions from animations, useAuth, useBrand, useToast, all catalog hooks and types. Icons: Plus, Eye, Pencil, Trash2, Calendar, Layers, Utensils, Tag, Globe, ChevronRight.

PAGE_SIZE = 10 constant at top.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors in catalogos/page.tsx. Run `npm run build` or `npx next build` — page compiles without errors. Visually: the page has 5 tabs, Temporadas/Tipos/Regimenes tabs render tables with working search, pagination, create/edit/delete modals.
  </verify>
  <done>Temporadas, Tipos de Paquete, and Regimenes tabs render with glass tables, working search/pagination, and modal CRUD (create, edit, delete with shake animation). Tab indicator animates with violet-to-teal gradient.</done>
</task>

<task type="auto">
  <name>Task 2: Etiquetas tab (color swatch + slug) and Paises y Ciudades tab (two-level CRUD)</name>
  <files>src/app/(admin)/catalogos/page.tsx</files>
  <action>
Add the remaining two tab components to the same catalogos/page.tsx file:

5. **EtiquetasTab function component** — similar modal CRUD pattern but with color and slug fields:
   - Hooks: useEtiquetas(), useCatalogActions() for createEtiqueta/updateEtiqueta/deleteEtiqueta
   - Form: {nombre, slug, color} — color defaults to "#8B5CF6" on create
   - **Slug auto-generation:** On create, when nombre changes, auto-generate slug: `nombre.toLowerCase().replace(/\s+/g, '-').replace(/[^a-z0-9-]/g, '')`. On edit, show existing slug but do NOT auto-overwrite (URL stability).
   - **Color swatch in table:** In the Nombre column cell, render a flex row with a 16x16 rounded-full span with `style={{ background: etiqueta.color }}` + border border-neutral-200/60, followed by the nombre text
   - **Slug column:** Show slug in table as secondary column with `className="font-mono text-xs text-neutral-400"`
   - Table columns: Nombre (with color swatch), Slug, Acciones
   - **Modal form fields:** Input for nombre (with onChange that also sets slug on create only: `if (!editTarget) setForm(f => ({...f, nombre: v, slug: slugify(v)}))`), Input for slug (editable override), `<input type="color">` styled with `className="h-10 w-full cursor-pointer rounded-glass-sm border border-neutral-200/80 bg-white/70"` and a label
   - Empty state icon: Tag from lucide-react
   - SearchFilter placeholder: "Buscar etiqueta..."

6. **PaisesTab function component** — two-level CRUD with expandable rows:
   - Hooks: usePaises() (returns (Pais & {ciudades: Ciudad[]})[] ), useCatalogActions() for createPais/updatePais/deletePais/createCiudad/updateCiudad/deleteCiudad
   - **Pais-level state:** paisModalOpen, editPais (Pais|null), deletePaisTarget, isShaking, paisForm ({nombre, codigo}), search, page
   - **Expand state:** expandedPaisId (string|null) — toggle via chevron button
   - **Ciudad inline state:** addingCiudad (string|null = paisId when adding), newCiudadNombre (string), editingCiudadId (string|null), ciudadDraft (string)
   - **Pais table columns:** Expand chevron (ChevronRight with rotate-90 transition when expanded), Nombre, Codigo (font-mono text-xs text-neutral-400), Ciudades count (text-neutral-400 text-sm: "{n} ciudades"), Acciones (Eye/Pencil/Trash2)
   - **Expanded row:** When expandedPaisId === pais.id, render a `<tr>` with `<td colSpan={5}>` below the pais row. Inside: a mini list of ciudades with each ciudad showing nombre + inline edit (Pencil icon toggles to Input + Check/X) + delete (Trash2, no confirmation needed for ciudades). At bottom: "+ Agregar ciudad" button that shows inline Input + Check/X.
   - **Ciudad inline edit:** Click Pencil on a ciudad -> setEditingCiudadId(ciudad.id) + setCiudadDraft(ciudad.nombre). Show Input + Check (saves via updateCiudad({...ciudad, nombre: ciudadDraft})) + X (cancels). Only one ciudad editable at a time.
   - **Ciudad inline add:** Click "+ Agregar ciudad" -> setAddingCiudad(pais.id) + setNewCiudadNombre(''). Show Input + Check (saves via createCiudad({paisId: pais.id, nombre: newCiudadNombre})) + X (cancels).
   - **Pais delete cascade:** In handleConfirmDeletePais, before deleting the pais, delete all its ciudades: `pais.ciudades.forEach(c => deleteCiudad(c.id))`. Then `deletePais(pais.id)`. Show warning in delete modal: "Se eliminaran {n} ciudades asociadas."
   - **Pais create/edit modal:** Same pattern as other tabs — Input for nombre, Input for codigo (placeholder "ej. UY, AR, BR")
   - Empty state icon: Globe from lucide-react
   - SearchFilter placeholder: "Buscar pais..."

**Ciudad styling in expanded section:**
- Background: `className="bg-neutral-50/30 px-8 pb-3 pt-1"`
- Each ciudad row: `className="flex items-center justify-between py-1.5 px-2 rounded hover:bg-neutral-100/40 transition-colors"`
- Ciudad nombre: `className="text-sm text-neutral-700"`
- Add button: small Button variant="ghost" size="sm" with Plus icon, text "Agregar ciudad"
- Inline input: Input size="sm" with auto-focus

**IMPORTANT:** Ciudad has no brandId — it inherits brand scope through parent Pais. When creating a ciudad, only pass {paisId, nombre} (no brandId).
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Run the dev server and navigate to /catalogos. Switch between all 5 tabs — each renders its content. Etiquetas: color swatches visible, slug auto-generates on create. Paises: expand a pais, see ciudades list, add/edit/delete ciudades inline. Create and delete a pais — ciudades cascade-deleted.
  </verify>
  <done>All 5 catalog tabs are fully functional: Temporadas, Tipos de Paquete, Etiquetas (with color swatches and slug auto-generation), Paises y Ciudades (with expandable two-level CRUD), and Regimenes. Each tab supports create, edit, delete with search/pagination and toast feedback.</done>
</task>

</tasks>

<verification>
1. Navigate to /catalogos — page renders with PageHeader and 5 tabs
2. Click each tab — gradient indicator animates smoothly between tabs (no layout jump)
3. In each tab:
   a. Table renders with correct columns and data from the active brand
   b. Search filters the list instantly, pagination resets to page 1
   c. "Nuevo {entity}" button opens create modal with empty/default fields
   d. Clicking Eye/Pencil opens edit modal with pre-populated fields
   e. Clicking Trash opens delete modal with shake animation on confirm
   f. Toast appears after each CRUD operation
   g. VENDEDOR cannot see create/edit/delete buttons (canEdit gate)
4. Etiquetas: color swatch renders next to nombre, slug auto-generates on create, color picker works
5. Paises: chevron expands to show ciudades, inline add/edit/delete works, pais delete cascades to ciudades
6. Switch brand in topbar — catalog data changes to reflect the new brand
</verification>

<success_criteria>
- All 5 catalog tabs render with glass tables, search, pagination, and modal CRUD
- Tab switching shows animated violet-to-teal gradient indicator
- Etiquetas display color swatches and auto-generate slugs
- Paises support two-level CRUD with expandable ciudad management
- All CRUD actions produce toast notifications
- Brand switching filters all catalog data correctly
- VENDEDOR role sees read-only view (no action buttons)
</success_criteria>

<output>
After completion, create `.planning/phases/07-catalogos-perfiles/07-01-SUMMARY.md`
</output>
