---
phase: 03-data-layer-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/utils.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-05

must_haves:
  truths:
    - "All 14 primary entity interfaces are importable from src/lib/types.ts with zero any types"
    - "All 8 junction/sub-entity interfaces are importable from src/lib/types.ts"
    - "formatCurrency(1234) returns '$1,234' and calcularVenta(1000, 35) returns 1350"
    - "slugify('Lunas de Miel') returns 'lunas-de-miel'"
    - "calcularNeto sums costs correctly from aereo prices, hotel-per-night, traslado flat, seguro-per-day, and circuito prices"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "All 22 TypeScript entity interfaces plus enum types"
      min_lines: 200
    - path: "src/lib/utils.ts"
      provides: "formatCurrency, calcularNeto, calcularVenta, slugify utility functions"
      exports: ["formatCurrency", "calcularNeto", "calcularVenta", "slugify"]
  key_links:
    - from: "src/lib/types.ts"
      to: "src/lib/utils.ts"
      via: "calcularNeto imports Paquete, Aereo, PrecioAereo, Alojamiento, PrecioAlojamiento, Traslado, Seguro, Circuito, PrecioCircuito"
      pattern: "import.*from.*types"
---

<objective>
Define all 22 TypeScript entity interfaces and 4 utility functions that form the foundation of the TravelOz data layer.

Purpose: Every subsequent plan in Phase 3 (seed data, context providers) imports from these two files. They must be correct and complete before anything else can be built.
Output: src/lib/types.ts (all entity interfaces), src/lib/utils.ts (formatting, pricing, slug utilities)
</objective>

<execution_context>
@/Users/diegothx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diegothx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-layer-types/03-RESEARCH.md

@src/lib/auth.ts
@src/lib/brands.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all TypeScript entity interfaces in types.ts</name>
  <files>src/lib/types.ts</files>
  <action>
Create src/lib/types.ts with ALL 22 entity interfaces derived from the Prisma schemas documented in the research. Use a single file to avoid circular import issues (Paquete references every service type).

**Enum types (at top of file):**
- `EstadoPaquete = 'BORRADOR' | 'ACTIVO' | 'INACTIVO'`
- `TipoTraslado = 'REGULAR' | 'PRIVADO'`

**Primary entities (14 interfaces) -- each MUST include these common fields:**
- `id: string`
- `brandId: string` (except Ciudad which inherits brand scope through Pais)
- `createdAt: string` (ISO 8601 string, not Date object)
- `updatedAt: string` (ISO 8601 string)
- `deletedAt: string | null` (for soft-deletable entities: Paquete, Aereo, Alojamiento, Traslado, Seguro, Circuito, Proveedor)

**Entity-specific fields (from research Entity Model Reference):**

1. `Paquete`: titulo, descripcion, textoVisual (string|null), noches (number), salidas (string -- comma separated departure dates text), temporadaId, tipoPaqueteId, validezDesde (string ISO), validezHasta (string ISO), estado (EstadoPaquete), destacado (boolean), netoCalculado (number), markup (number -- percentage), precioVenta (number), moneda (string -- always 'USD'), ordenServicios (string[] -- ordered service IDs)
2. `Aereo`: ruta (string -- e.g. "MVD -> GIG -> MVD"), destino (string), aerolinea (string), equipaje (string)
3. `Alojamiento`: nombre, ciudadId, paisId, categoria (number -- star rating 1-5), sitioWeb (string|null)
4. `Traslado`: nombre, tipo (TipoTraslado), ciudadId, paisId, proveedorId, precio (number)
5. `Seguro`: proveedorId, plan (string), cobertura (string -- e.g. "USD 40.000"), costoPorDia (number)
6. `Circuito`: nombre, noches (number), proveedorId
7. `Proveedor`: nombre, contacto (string|null), email (string|null), telefono (string|null), notas (string|null)
8. `Temporada`: nombre, orden (number), activa (boolean) -- NO deletedAt (reference data)
9. `TipoPaquete`: nombre, orden (number), activo (boolean) -- NO deletedAt
10. `Etiqueta`: nombre, slug (string), color (string -- hex) -- NO deletedAt
11. `Pais`: nombre, codigo (string -- ISO country code) -- NO deletedAt
12. `Ciudad`: paisId (string), nombre -- NO brandId, NO deletedAt
13. `Regimen`: nombre, abrev (string -- abbreviation e.g. "AI") -- NO deletedAt
14. `User`: Already exists as AuthUser in auth.ts. Do NOT redefine. Export a re-export: `export type { AuthUser as User } from './auth'`

**Junction/Sub-entities (8 interfaces):**

15. `PrecioAereo`: id, aereoId, periodoDesde (string ISO), periodoHasta (string ISO), precioAdulto (number), precioMenor (number)
16. `PrecioAlojamiento`: id, alojamientoId, periodoDesde, periodoHasta, precioPorNoche (number), regimenId (string)
17. `PrecioCircuito`: id, circuitoId, periodoDesde, periodoHasta, precio (number)
18. `CircuitoDia`: id, circuitoId, numeroDia (number), titulo (string), descripcion (string), orden (number)
19. `AlojamientoFoto`: id, alojamientoId, url (string), alt (string), orden (number)
20. `PaqueteFoto`: id, paqueteId, url (string), alt (string), orden (number)
21. `PaqueteEtiqueta`: id, paqueteId, etiquetaId
22. `PaqueteServicio` -- define 5 specific types:
    - `PaqueteAereo`: id, paqueteId, aereoId, textoDisplay (string|null), orden (number)
    - `PaqueteAlojamiento`: id, paqueteId, alojamientoId, nochesEnEste (number|null), textoDisplay (string|null), orden (number)
    - `PaqueteTraslado`: id, paqueteId, trasladoId, textoDisplay (string|null), orden (number)
    - `PaqueteSeguro`: id, paqueteId, seguroId, diasCobertura (number|null), textoDisplay (string|null), orden (number)
    - `PaqueteCircuito`: id, paqueteId, circuitoId, textoDisplay (string|null), orden (number)

**Rules:**
- Zero `any` types anywhere in the file
- All dates stored as ISO 8601 strings (mirrors real API behavior)
- Add JSDoc comments above each primary entity interface with a 1-line description
- Export everything as named exports
  </action>
  <verify>Run `npx tsc --noEmit` and confirm zero errors. Grep for `any` in types.ts and confirm zero matches.</verify>
  <done>All 22 entity interfaces are defined with proper fields, zero any types, and the file compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create utility functions in utils.ts</name>
  <files>src/lib/utils.ts</files>
  <action>
Create src/lib/utils.ts with 4 exported utility functions. Import necessary types from ./types.

**1. formatCurrency(amount: number): string**
```typescript
const usdFormatter = new Intl.NumberFormat('en-US', {
  style: 'currency',
  currency: 'USD',
  minimumFractionDigits: 0,
  maximumFractionDigits: 0,
});
export function formatCurrency(amount: number): string {
  return usdFormatter.format(Math.round(amount));
}
```
- formatCurrency(1234.5) must return "$1,235"
- formatCurrency(0) must return "$0"

**2. calcularNeto(...)**: number
Signature from research:
```typescript
export function calcularNeto(
  paquete: Paquete,
  assignedAereos: { aereo: Aereo; precioAereo?: PrecioAereo }[],
  assignedAlojamientos: { alojamiento: Alojamiento; precioAlojamiento?: PrecioAlojamiento; nochesEnEste?: number }[],
  assignedTraslados: Traslado[],
  assignedSeguros: { seguro: Seguro; diasCobertura?: number }[],
  assignedCircuitos: { circuito: Circuito; precioCircuito?: PrecioCircuito }[],
): number
```
Logic:
- Aereos: sum precioAdulto for each assigned (use precioAereo?.precioAdulto ?? 0)
- Alojamientos: precioPorNoche * noches (use nochesEnEste ?? paquete.noches as fallback)
- Traslados: sum precio (flat amount per traslado)
- Seguros: costoPorDia * dias (use diasCobertura ?? paquete.noches as fallback)
- Circuitos: sum precioCircuito?.precio ?? 0

**3. calcularVenta(neto: number, markup: number): number**
Formula: `Math.round(neto * (1 + markup / 100))`
- calcularVenta(1000, 35) must return 1350
- calcularVenta(0, 50) must return 0

**4. slugify(text: string): string**
```typescript
export function slugify(text: string): string {
  return text
    .toLowerCase()
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '');
}
```
- slugify("Black Friday 2026") must return "black-friday-2026"
- slugify("Lunas de Miel") must return "lunas-de-miel"
- slugify("Promo Nordeste Marzo") must return "promo-nordeste-marzo"
- Handles Spanish characters: n with tilde, accented vowels

**Note:** Do NOT create a `cn()` utility here -- it already exists at `@/components/lib/cn`.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm clean compilation. Manually verify in terminal: `npx tsx -e "const { formatCurrency, calcularVenta, slugify } = require('./src/lib/utils'); console.log(formatCurrency(1234.5), calcularVenta(1000,35), slugify('Lunas de Miel'))"` or equivalent quick check.</verify>
  <done>All 4 utility functions exported from utils.ts, TypeScript compiles cleanly, formatCurrency returns "$1,235" for 1234.5, calcularVenta(1000,35) returns 1350, slugify handles Spanish characters.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `grep -c 'any' src/lib/types.ts` returns 0
- types.ts exports all 22+ interfaces (14 primary + 8 junction + enum types)
- utils.ts exports formatCurrency, calcularNeto, calcularVenta, slugify
- No circular imports between types.ts and utils.ts (types.ts has no imports from lib/)
</verification>

<success_criteria>
- All entity interfaces defined matching the research Entity Model Reference
- All utility functions work correctly with expected inputs
- Zero TypeScript errors, zero `any` types
- Files are importable by subsequent plans (seed data, providers)
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-layer-types/03-01-SUMMARY.md`
</output>
