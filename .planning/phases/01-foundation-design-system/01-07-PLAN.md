---
phase: 01-foundation-design-system
plan: 07
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/ui/Tag.tsx
  - src/components/ui/Skeleton.tsx
  - src/components/ui/Avatar.tsx
  - src/components/ui/Breadcrumb.tsx
autonomous: true
requirements:
  - DSYS-16
  - DSYS-17
  - DSYS-18
  - DSYS-19

must_haves:
  truths:
    - "Tags render as removable pills in 6 color presets with backdrop-filter blur"
    - "Skeleton renders with shimmer animation for loading states"
    - "Avatar renders in 4 sizes with pill border-radius and fallback initials"
    - "Breadcrumb renders with / separator, muted color, and teal hover on links"
  artifacts:
    - path: "src/components/ui/Tag.tsx"
      provides: "Tag pills in 6 colors with removable X"
      exports: ["Tag"]
    - path: "src/components/ui/Skeleton.tsx"
      provides: "Shimmer skeleton loader"
      exports: ["Skeleton"]
    - path: "src/components/ui/Avatar.tsx"
      provides: "Avatar with 4 sizes and fallback initials"
      exports: ["Avatar"]
    - path: "src/components/ui/Breadcrumb.tsx"
      provides: "Breadcrumb navigation"
      exports: ["Breadcrumb"]
  key_links:
    - from: "src/components/ui/Tag.tsx"
      to: "src/components/lib/cn.ts"
      via: "cn utility for class merging"
      pattern: "import.*cn"
---

<objective>
Build the simple display components: tag pills, skeleton loaders, avatars, and breadcrumbs. These are smaller building blocks used across the admin.

Purpose: Tags appear on paquetes (etiquetas), skeletons handle loading states, avatars show in user menus and profiles, breadcrumbs navigate in topbar.
Output: 4 lightweight display components with correct design.json styling.
</objective>

<execution_context>
@/Users/diegothx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diegothx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/design.json
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Tag pills and Skeleton loader</name>
  <files>src/components/ui/Tag.tsx, src/components/ui/Skeleton.tsx</files>
  <action>
**Tag.tsx:**

1. Props:
   ```typescript
   interface TagProps {
     children: React.ReactNode;
     color?: 'teal' | 'violet' | 'red' | 'orange' | 'green' | 'blue';
     removable?: boolean;
     onRemove?: () => void;
     className?: string;
   }
   ```

2. **Color presets** (from design.json components.tag.presets):
   - teal: bg rgba(230,248,245,0.7), text #1F7D70
   - violet: bg rgba(245,243,255,0.7), text #5B21B6
   - red: bg rgba(255,224,227,0.7), text #A8192A
   - orange: bg rgba(254,245,235,0.7), text #A85C18
   - green: bg rgba(237,250,243,0.7), text #166B40
   - blue: bg rgba(235,245,255,0.7), text #0066E6

3. **Styling**:
   - Base classes: `inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-[11px] font-medium`
   - Inline style: `{ backdropFilter: 'blur(6px)', WebkitBackdropFilter: 'blur(6px)' }` + color-specific bg and text via style object
   - Default color: teal

4. **Removable**: If removable=true, render X button (lucide X icon, 12px) on right that calls onRemove. X button has hover opacity increase.

5. Use AnimatePresence on parent if you want tags to animate out. The Tag itself just renders -- parent manages list.

**Skeleton.tsx:**

1. Props:
   ```typescript
   interface SkeletonProps {
     className?: string;
     width?: string | number;
     height?: string | number;
     rounded?: 'sm' | 'md' | 'lg' | 'full';
   }
   ```

2. **Styling**:
   - Background: `linear-gradient(90deg, rgba(236,237,245,0.6) 0%, rgba(255,255,255,0.4) 50%, rgba(236,237,245,0.6) 100%)` via inline style
   - Background-size: `200% 100%`
   - Animation: `animate-shimmer` class (from tailwind config: shimmer 1.8s ease-in-out infinite)
   - Border-radius map: sm=8px, md=12px (default), lg=16px, full=9999px

3. Render as a `div` with the shimmer animation. Apply width/height from props or let className handle sizing.

4. Common pattern: `<Skeleton className="h-4 w-32" />` for text, `<Skeleton className="h-10 w-full" />` for input, `<Skeleton className="h-40 w-full" rounded="lg" />` for card.
  </action>
  <verify>
- Files compile: `npx tsc --noEmit src/components/ui/Tag.tsx src/components/ui/Skeleton.tsx`
- Tag has 6 colors: `grep -c "teal\|violet\|red\|orange\|green\|blue" src/components/ui/Tag.tsx` should return 6+
- Skeleton has shimmer: `grep "shimmer" src/components/ui/Skeleton.tsx`
  </verify>
  <done>
- Tag renders in 6 color presets with pill border-radius, backdrop-blur 6px, and optional removable X button
- Skeleton renders with shimmer gradient animation for loading states
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Avatar and Breadcrumb components</name>
  <files>src/components/ui/Avatar.tsx, src/components/ui/Breadcrumb.tsx</files>
  <action>
**Avatar.tsx:**

1. Props:
   ```typescript
   interface AvatarProps {
     src?: string;
     alt?: string;
     name?: string; // for fallback initials
     size?: 'xs' | 'sm' | 'md' | 'lg';
     className?: string;
   }
   ```

2. **Size map** (from design.json components.avatar.sizes):
   - xs: 24px, sm: 32px, md: 40px, lg: 48px

3. **Styling**:
   - Container: rounded-full (pill radius 9999px), overflow-hidden
   - Border: `2px solid rgba(255,255,255,0.5)` via inline style
   - Shadow: shadow-elevation-2

4. **Image**: If `src` provided, render `<img>` with `object-cover` class. Handle image load error by falling back to initials.

5. **Fallback initials**: If no src or image fails to load, show initials derived from `name` prop:
   - Take first letter of first two words (e.g., "Gerónimo Cassoni" -> "GC")
   - Display in a div with bg-brand-teal-100 text-brand-teal-600, centered, font-semibold
   - Font size: xs=text-[9px], sm=text-[11px], md=text-xs, lg=text-sm

6. Use `useState` to track image load error for fallback.

**Breadcrumb.tsx:**

1. Props:
   ```typescript
   interface BreadcrumbItem {
     label: string;
     href?: string; // if undefined, it's the current (last) item
   }
   interface BreadcrumbProps {
     items: BreadcrumbItem[];
     className?: string;
   }
   ```

2. **Styling** (from design.json components.breadcrumb):
   - Font: text-[12.5px]
   - Separator: "/" in text-neutral-300, mx-2
   - Link items: text-neutral-400 hover:text-brand-teal-500 transition-colors cursor-pointer
   - Current item (last, no href): text-neutral-700 font-medium

3. Render as `<nav aria-label="Breadcrumb">` with `<ol>` containing `<li>` items.

4. Link items: render as `<a>` or Next.js `Link` (use `<a>` for now since Next.js routing isn't set up yet in this phase -- the component can be updated later). Actually, accept an optional `onClick` per item or just render text for simplicity. The breadcrumb in the topbar will be composed at the layout level.

5. Layout: `flex items-center`
  </action>
  <verify>
- Files compile: `npx tsc --noEmit src/components/ui/Avatar.tsx src/components/ui/Breadcrumb.tsx`
- Avatar has 4 sizes: `grep "xs\|sm\|md\|lg" src/components/ui/Avatar.tsx`
- Breadcrumb has separator: `grep '"/"' src/components/ui/Breadcrumb.tsx`
  </verify>
  <done>
- Avatar renders in 4 sizes (24/32/40/48px) with pill border-radius, white border, elevation shadow, and fallback initials from name
- Breadcrumb renders with / separator, muted link color, teal hover, and bold current item
  </done>
</task>

</tasks>

<verification>
1. All 4 files compile with TypeScript
2. Tag has 6 color presets with removable option
3. Skeleton has shimmer animation
4. Avatar has 4 sizes with image + fallback
5. Breadcrumb has separator and current item styling
</verification>

<success_criteria>
- Tag in 6 colors with pill shape, backdrop-blur 6px, removable X button
- Skeleton with shimmer gradient animation (1.8s infinite)
- Avatar in 4 sizes (xs/sm/md/lg) with white border, elevation shadow, initials fallback
- Breadcrumb with / separator, neutral-400 links, teal hover, neutral-700 current
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-07-SUMMARY.md`
</output>
