---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/ui/Button.tsx
  - src/components/ui/Input.tsx
  - src/components/ui/Badge.tsx
autonomous: true
requirements:
  - DSYS-02
  - DSYS-03
  - DSYS-07

must_haves:
  truths:
    - "Clay buttons render with 3D gradient shadows, sheen hover animation, and press scale(0.96) for primary/danger variants"
    - "Glass inputs render with backdrop-filter blur, focus double-ring (white + teal), error shake animation, and all 6 states"
    - "Badges render in all 9 glass variants with correct background/text/border colors and 3 sizes"
  artifacts:
    - path: "src/components/ui/Button.tsx"
      provides: "Clay button component with 6 variants and 4 sizes"
      exports: ["Button", "buttonVariants"]
    - path: "src/components/ui/Input.tsx"
      provides: "Glass input with label, error text, all states"
      exports: ["Input"]
    - path: "src/components/ui/Badge.tsx"
      provides: "Badge with 9 glass variants and 3 sizes"
      exports: ["Badge"]
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "src/components/lib/animations.ts"
      via: "springs and interactions imports"
      pattern: "import.*animations"
    - from: "src/components/ui/Button.tsx"
      to: "src/components/lib/cn.ts"
      via: "cn utility import"
      pattern: "import.*cn"
    - from: "src/components/ui/Input.tsx"
      to: "src/components/lib/cn.ts"
      via: "cn utility import"
      pattern: "import.*cn"
---

<objective>
Build the three most-used atomic primitives: clay buttons, glass inputs, and glass badges. These appear on every page of the admin panel.

Purpose: Buttons, inputs, and badges are the building blocks composed into every form, table, and modal across all modules.
Output: 3 production-ready components with full variant systems, animations, and accessibility.
</objective>

<execution_context>
@/Users/diegothx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diegothx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/design.json
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build clay Button component with CVA variants and sheen hover</name>
  <files>src/components/ui/Button.tsx</files>
  <action>
Create `src/components/ui/Button.tsx` using CVA + Motion + cn():

1. **CVA variants** for Tailwind classes (sizing, layout, font):
   - `variant`: primary, danger, secondary, ghost, ghostDanger, icon
   - `size`: xs (h-7 px-2.5 text-[11px]), sm (h-[34px] px-3.5 text-[12.5px]), md (h-10 px-[18px] text-sm), lg (h-[46px] px-[22px] text-[15px])
   - Base classes: `inline-flex items-center justify-center gap-2 font-semibold rounded-clay transition-colors focus-visible:outline-none disabled:opacity-50 disabled:pointer-events-none`
   - secondary: `text-neutral-700 border border-neutral-200`
   - ghost: `text-neutral-600 hover:bg-neutral-100/60`
   - ghostDanger: `text-brand-red-500 hover:bg-brand-red-100/50`
   - icon: `text-neutral-400 hover:text-neutral-600 p-0 w-8 h-8` (override size)

2. **Inline clay styles** (cannot be in CVA -- require style prop):
   - primary: background `linear-gradient(145deg, #45D4C0 0%, #2A9E8E 100%)`, boxShadow `6px 6px 16px rgba(42,158,142,0.25), -3px -3px 10px rgba(69,212,192,0.3), inset 0 1px 0 rgba(255,255,255,0.35)`, color white
   - danger: background `linear-gradient(145deg, #E74C5F 0%, #CC2030 100%)`, boxShadow `6px 6px 16px rgba(204,32,48,0.25), -3px -3px 10px rgba(231,76,95,0.3), inset 0 1px 0 rgba(255,255,255,0.25)`, color white
   - secondary: apply frostedSubtle material from glass.ts

3. **Sheen hover effect** (for primary and danger):
   - Use `::after` pseudo-element with `overflow-hidden` on button
   - Sheen gradient: `linear-gradient(105deg, transparent 40%, rgba(255,255,255,0.15) 45%, rgba(255,255,255,0.25) 50%, rgba(255,255,255,0.15) 55%, transparent 60%)`
   - Apply via Tailwind `after:` classes with `animate-sheen-slide` or inline backgroundPosition animation
   - Alternative simpler approach: use CSS `background-size: 200% 100%` + sheenSlide animation on hover via a conditional class

4. **Motion animation**: Wrap in `motion.button` with:
   - `whileTap={{ scale: 0.96 }}` (from interactions.buttonPress)
   - `whileHover={{ scale: 1.01, y: -1 }}` (from interactions.buttonPress)
   - `transition={springs.snappy}`

5. **Props**: Extend `React.ButtonHTMLAttributes<HTMLButtonElement>` + `VariantProps<typeof buttonVariants>` + optional `loading?: boolean` (shows Loader2 spinner) and `leftIcon?: React.ReactNode`, `rightIcon?: React.ReactNode`.

6. Use `React.forwardRef` for Radix `asChild` compatibility.

Export both `Button` component and `buttonVariants` (for reuse in other components).
  </action>
  <verify>
- File exists and TypeScript compiles: `npx tsc --noEmit src/components/ui/Button.tsx`
- Exports Button and buttonVariants: `grep "export" src/components/ui/Button.tsx`
  </verify>
  <done>
- Button renders with 6 CVA variants (primary/danger/secondary/ghost/ghostDanger/icon) and 4 sizes (xs/sm/md/lg)
- Primary and danger use clay inline styles with 3D shadows from design.json
- Sheen hover animation present on primary/danger variants
- Motion whileTap scale(0.96) and whileHover scale(1.01) with snappy spring
- forwardRef for Radix asChild compatibility
  </done>
</task>

<task type="auto">
  <name>Task 2: Build glass Input and Badge components</name>
  <files>src/components/ui/Input.tsx, src/components/ui/Badge.tsx</files>
  <action>
**Input.tsx:**

1. Create glass Input component with:
   - Props: extend `React.InputHTMLAttributes<HTMLInputElement>` + `label?: string`, `error?: string`, `leftIcon?: React.ReactNode`, `rightIcon?: React.ReactNode`, `size?: 'sm' | 'md' | 'lg'`
   - Use `React.forwardRef`

2. **Glass styling** (inline style object for base):
   - background: `rgba(255,255,255,0.7)`, backdropFilter: `blur(8px)`, WebkitBackdropFilter: `blur(8px)`
   - border: `1px solid rgba(228,230,242,0.8)`
   - boxShadow: `inset 0 2px 4px rgba(26,26,46,0.04), 0 1px 2px rgba(26,26,46,0.04)`

3. **Size classes** via CVA or conditional cn():
   - sm: h-[34px] px-3 text-[12.5px]
   - md: h-10 px-3.5 text-sm (default)
   - lg: h-[46px] px-[18px] text-[15px]

4. **State handling** -- apply different styles based on state:
   - Base: rounded-clay font-body text-neutral-900 placeholder:text-neutral-400 transition-all
   - Hover: border-neutral-300 (use peer/group or CSS hover)
   - Focus: borderColor `#3BBFAD`, boxShadow `0 0 0 2px rgba(255,255,255,0.8), 0 0 0 4px rgba(59,191,173,0.4)`, bg `rgba(255,255,255,0.85)`. Use `focus-within:` classes on wrapper.
   - Error: borderColor `#E74C5F`, boxShadow `0 0 0 2px rgba(255,255,255,0.8), 0 0 0 4px rgba(231,76,95,0.3)`, bg `rgba(255,240,241,0.7)`. Apply microBounce animation to error text.
   - Disabled: bg `rgba(236,237,245,0.5)`, color `#B0B4CD`, cursor not-allowed
   - ReadOnly: bg `rgba(245,246,250,0.6)`

5. **Label**: render above input with fontSize 12.5px, fontWeight 500, color #2D2F4D, marginBottom 6px
6. **Error text**: render below input with fontSize 11px, color #CC2030, fontWeight 500, with microBounce animation class

**Badge.tsx:**

1. Create Badge component with CVA:
   - `variant`: 9 variants from design.json -- confirmed, pending, draft, active, inactive, removed, new, temporada, promo
   - `size`: sm (h-5 px-1.5 text-[10px]), md (h-6 px-2.5 text-[11px]), lg (h-7 px-3 text-[12.5px])
   - Base: `inline-flex items-center justify-center font-semibold rounded-sm`

2. **Inline styles per variant** (glass backdrop + exact colors from design.json):
   - confirmed: bg rgba(237,250,243,0.8), text #1F8A54, border 1px solid rgba(46,173,107,0.15)
   - pending: bg rgba(254,245,235,0.8), text #D07520, border 1px solid rgba(232,145,58,0.15)
   - draft: bg rgba(236,237,245,0.6), text #6B6F99 (no border)
   - active: bg rgba(230,248,245,0.8), text #1F7D70, border 1px solid rgba(59,191,173,0.15)
   - inactive: bg rgba(236,237,245,0.5), text #8A8DB5
   - removed: bg rgba(255,224,227,0.6), text #CC2030, textDecoration line-through
   - new: bg rgba(230,248,245,0.8), text #1F7D70 (with microBounce animation)
   - temporada: bg rgba(255,251,235,0.8), text #D4A800, border 1px solid rgba(240,192,0,0.15)
   - promo: bg rgba(255,224,227,0.8), text #A8192A, border 1px solid rgba(204,32,48,0.15)

3. Apply `backdropFilter: 'blur(8px)'` and `WebkitBackdropFilter: 'blur(8px)'` to all badges via inline style.

4. Props: `variant`, `size`, `children`, `className` for overrides. No forwardRef needed.
  </action>
  <verify>
- Both files compile: `npx tsc --noEmit src/components/ui/Input.tsx src/components/ui/Badge.tsx`
- Input exports: `grep "export" src/components/ui/Input.tsx`
- Badge has 9 variants: `grep -c "confirmed\|pending\|draft\|active\|inactive\|removed\|new\|temporada\|promo" src/components/ui/Badge.tsx`
  </verify>
  <done>
- Input renders with glass background, 6 states (default/hover/focus/error/disabled/readOnly), focus double-ring teal, error shake, label and error text
- Badge renders in all 9 glass variants with backdrop-filter blur(8px), correct colors per variant, and 3 sizes
- Both use cn() for class merging and exact design.json token values
  </done>
</task>

</tasks>

<verification>
1. Button.tsx, Input.tsx, Badge.tsx all exist and compile with TypeScript
2. Button has 6 CVA variants, clay inline styles for primary/danger, Motion animation
3. Input has glass styling, focus teal ring, error state with shake
4. Badge has all 9 variants with exact colors from design.json
</verification>

<success_criteria>
- Button renders with clay 3D shadows, sheen hover, press scale(0.96), 6 variants, 4 sizes
- Input renders with glass backdrop-filter, 6 states including focus double-ring and error shake
- Badge renders in 9 glass variants with backdrop-blur and 3 sizes
- All imports resolve correctly to lib/cn, lib/glass, lib/animations
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md`
</output>
