---
phase: 05-aereos-alojamientos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/alojamientos/page.tsx
  - src/app/(admin)/alojamientos/nuevo/page.tsx
  - src/app/(admin)/alojamientos/[id]/page.tsx
autonomous: true
requirements: [ALOJ-01, ALOJ-02, ALOJ-03, ALOJ-04]

must_haves:
  truths:
    - "Alojamientos list renders glass table with columns: ID, hotel, ciudad, pais, categoria (estrellas), acciones"
    - "Star rating renders as 5 amber Star icons (filled vs outline) based on categoria value 1-5"
    - "User can clone an alojamiento — 'Copia de {nombre}' appears in list with success toast"
    - "User can delete an alojamiento via shake-animated modal — row disappears with toast"
    - "User can create a new alojamiento via /alojamientos/nuevo with pais/ciudad cascading selects"
    - "Alojamiento detail page shows hotel fields in an editable form card"
    - "Detail page shows a price-per-period table with regimen dropdown in edit mode"
    - "User can add, edit inline, and delete price rows with regimen selection"
    - "Detail page shows a photo grid powered by ImageUploader with add/remove/reorder"
    - "VENDEDOR sees no action buttons and no editable fields or photo upload controls"
  artifacts:
    - path: "src/app/(admin)/alojamientos/page.tsx"
      provides: "Alojamientos list with glass table, star rating, search, clone/delete CRUD"
      min_lines: 180
    - path: "src/app/(admin)/alojamientos/nuevo/page.tsx"
      provides: "Create alojamiento form with cascading pais/ciudad selects — static route before [id]"
      min_lines: 80
    - path: "src/app/(admin)/alojamientos/[id]/page.tsx"
      provides: "Alojamiento detail with form + price table (regimen select) + ImageUploader photo grid"
      min_lines: 280
  key_links:
    - from: "src/app/(admin)/alojamientos/page.tsx"
      to: "useAlojamientos(), usePaises()"
      via: "ServiceProvider + CatalogProvider hooks"
      pattern: "useAlojamientos|usePaises"
    - from: "alojamientos list ciudad/pais columns"
      to: "ciudadMap, paisMap"
      via: "useMemo lookup from usePaises() nested ciudades"
      pattern: "ciudadMap|paisMap"
    - from: "src/app/(admin)/alojamientos/[id]/page.tsx"
      to: "useServiceState().preciosAlojamiento"
      via: "filter by alojamientoId"
      pattern: "preciosAlojamiento\\.filter\\(p => p\\.alojamientoId"
    - from: "inline price table regimen select"
      to: "useRegimenes()"
      via: "CatalogProvider hook"
      pattern: "useRegimenes"
    - from: "photo grid"
      to: "alojamientoFotos, createAlojamientoFoto, deleteAlojamientoFoto, updateAlojamientoFoto"
      via: "useServiceState + useServiceActions"
      pattern: "alojamientoFotos|createAlojamientoFoto"
---

<objective>
Build the complete Alojamientos module: list page with glass table (including star rating and ciudad/pais lookup), a /alojamientos/nuevo create page with cascading pais/ciudad selects, and a detail/edit page at /alojamientos/[id] with an inline-editable price table (including regimen select) and a photo grid.

Purpose: Users can browse, search, create, edit, clone, and delete hotel entities. The detail page provides period-based pricing with regimen selection (the more complex variant of the inline price table from plan 05-01) and photo management consistent with the Paquetes FotosTab pattern.
Output: Three page files replacing the placeholder and adding new routes for the Alojamientos module.
</objective>

<execution_context>
@/Users/diegothx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diegothx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-aereos-alojamientos/05-RESEARCH.md

# Key source files to mirror
@src/app/(admin)/paquetes/page.tsx
@src/app/(admin)/paquetes/[slug]/_components/DatosTab.tsx
@src/app/(admin)/paquetes/[slug]/_components/FotosTab.tsx
@src/components/providers/ServiceProvider.tsx
@src/components/providers/CatalogProvider.tsx
@src/lib/types.ts
@src/components/lib/animations.ts
@src/components/lib/glass.ts
@src/components/ui/ImageUploader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alojamientos list page and /alojamientos/nuevo create page</name>
  <files>
    src/app/(admin)/alojamientos/page.tsx
    src/app/(admin)/alojamientos/nuevo/page.tsx
  </files>
  <action>
Replace the placeholder `src/app/(admin)/alojamientos/page.tsx` with a full list page mirroring the Aereos list from 05-01, adapted for Alojamientos complexity.

**List page (`alojamientos/page.tsx`):**
- Import `useAlojamientos` from ServiceProvider; `usePaises` from CatalogProvider
- Build two lookup maps in `useMemo` (MUST flatten nested structure — `usePaises()` returns `(Pais & { ciudades: Ciudad[] })[]`):
  ```
  paisMap: Record<string, string>  // paisId → nombre
  ciudadMap: Record<string, string> // ciudadId → nombre
  ```
  Iterate `paises` for paisMap; iterate `pais.ciudades` for ciudadMap. Not doing this causes ciudad column to always show "--".
- Table columns: ID (short UUID prefix), Hotel (alojamiento.nombre), Ciudad (ciudadMap[ciudadId] ?? "--"), Pais (paisMap[paisId] ?? "--"), Categoria (StarRating component, see below), Acciones
- **StarRating inline component**: Render 5 `Star` icons from lucide-react. Index < categoria → `fill-amber-400 text-amber-400`; otherwise → `text-neutral-600` (outline/dim). Size: `h-3 w-3`. All 5 always shown.
- Search: filter by `alojamiento.nombre`, ciudad name, pais name (use pre-computed lookup maps for city/country search)
- Clone: copies `{brandId, nombre: "Copia de {nombre}", ciudadId, paisId, categoria, sitioWeb}`, calls `createAlojamiento(...)`, shows toast
- Delete: same shake modal pattern as Paquetes/Aereos. Calls `deleteAlojamiento(id)`, shows toast
- VENDEDOR: hides Nuevo button and Copy/Trash action buttons
- Pagination: 10 items/page with `Pagination` component
- PageHeader: title "Alojamientos", icon `Hotel` from lucide-react
- Empty state: centered `Hotel` icon, "No hay alojamientos registrados"
- Navigation: Eye/Pencil → `/alojamientos/{id}`, New → `/alojamientos/nuevo`

**Create page (`alojamientos/nuevo/page.tsx`):**
- VENDEDOR guard: `useEffect` redirect to `/alojamientos` if !canEdit
- Import `usePaises` from CatalogProvider for pais/ciudad selects
- Form fields:
  - `nombre` — text input
  - `paisId` — Select from `usePaises()` options `[{ value: p.id, label: p.nombre }]`
  - `ciudadId` — Select filtered by selected paisId: `paises.find(p => p.id === paisId)?.ciudades.map(c => ({ value: c.id, label: c.nombre })) ?? []`. Reset `ciudadId` to `""` when `paisId` changes (use `useEffect` or inline handler)
  - `categoria` — number select 1-5 (or radio/select with star labels: "1 estrella" to "5 estrellas"), default 3
  - `sitioWeb` — text input (optional), placeholder "https://..."
- Default state: `{ nombre: "", paisId: "", ciudadId: "", categoria: 3, sitioWeb: "" }`
- On save: call `createAlojamiento({ brandId: activeBrandId, nombre, ciudadId, paisId, categoria, sitioWeb })`, redirect to `/alojamientos`
- Cancel: navigate to `/alojamientos`
- Glass card layout using `glassMaterials.frosted` inline style
- `"use client"` directive
  </action>
  <verify>
    Navigate to /alojamientos — glass table renders with Hotel, Ciudad, Pais, Categoria columns. Star rating shows correct filled/outline icons for each row's categoria value. Ciudad and Pais columns show names (not IDs or "--"). Search by hotel name filters correctly. Clone → "Copia de {nombre}" in table with toast. Delete modal shake → alojamiento removed. Navigate to /alojamientos/nuevo — form renders with pais select; selecting a pais populates ciudad select; changing pais resets ciudad.
  </verify>
  <done>
    Alojamientos list shows all brand-filtered alojamientos with correct ciudad/pais names and star ratings; clone/delete work; /alojamientos/nuevo renders with cascading pais/ciudad selects that reset correctly; VENDEDOR sees read-only table.
  </done>
</task>

<task type="auto">
  <name>Task 2: Alojamiento detail page with form, inline price table (regimen), and photo grid</name>
  <files>
    src/app/(admin)/alojamientos/[id]/page.tsx
  </files>
  <action>
Create `src/app/(admin)/alojamientos/[id]/page.tsx` — the detail/edit page for a single Alojamiento, consisting of three stacked cards:

**Card 1 — Hotel data form:**
- `useParams<{ id: string }>()` to get id
- Look up: `useServiceState().alojamientos.find(a => a.id === id && !a.deletedAt)` — if not found, "Alojamiento no encontrado" with back button
- Local `useState` for form: `{ nombre, ciudadId, paisId, categoria, sitioWeb }`
- Import `usePaises` for cascading pais/ciudad selects (same cascade logic as /nuevo page — reset ciudadId when paisId changes)
- Inputs: nombre (text), paisId (Select), ciudadId (Select filtered by paisId), categoria (Select 1-5), sitioWeb (text)
- Save calls `updateAlojamiento({ ...alojamiento, ...formState })`, shows toast "Alojamiento actualizado"
- Back button → `/alojamientos`
- PageHeader with title = alojamiento.nombre
- VENDEDOR: all inputs `readOnly`, Save hidden. For Select components, use `disabled` prop when !canEdit.

**Card 2 — Inline price-per-period table (with regimen):**
- Import `useRegimenes` from CatalogProvider for the regimen dropdown
- Read prices: `useServiceState().preciosAlojamiento.filter(p => p.alojamientoId === alojamiento.id)`
- Local inline-edit state (same 4-state pattern as Aereos plan 05-01):
  ```
  editingRowId, draftRow, addingRow, newRow
  ```
- Table columns: Periodo Desde, Periodo Hasta, Precio/Noche (USD), Regimen, Acciones
- **View mode**: show formatted values; regimen name from `regimenes.find(r => r.id === row.regimenId)?.nombre ?? "--"`; Edit/Delete buttons
- **Edit mode** (editingRowId === row.id):
  - `<input type="date">` for periodoDesde and periodoHasta bound to `draftRow`
  - `<input type="number">` for precioPorNoche bound to `draftRow`
  - Select for regimenId: use existing `Select` component from `@/components/ui/Select` with `options={regimenes.map(r => ({ value: r.id, label: r.nombre }))}`, bound to `draftRow.regimenId`
  - Save: call `updatePrecioAlojamiento(draftRow as PrecioAlojamiento)`, reset BOTH `editingRowId` to null AND `draftRow` to `{}` (critical — prevents edit state desync), show toast
  - Cancel: reset both
- **Add row**: "+ Agregar periodo" button → `addingRow = true` → empty editable row at bottom. Fields: date inputs + number + regimen Select. Save: `createPrecioAlojamiento({ alojamientoId: alojamiento.id, periodoDesde, periodoHasta, precioPorNoche, regimenId })`, reset addingRow/newRow, toast. Cancel: reset.
- **Delete row**: Direct `deletePrecioAlojamiento(id)`, toast "Periodo eliminado" — no modal
- VENDEDOR: all edit/add/delete controls hidden; table read-only
- Card title: "Precios por Periodo"

**Card 3 — Photo grid:**
- Mirror `FotosTab.tsx` exactly, adapted for `AlojamientoFoto`:
  - `const fotos = useServiceState().alojamientoFotos.filter(f => f.alojamientoId === alojamiento.id)`
  - `const images: ImageItem[] = fotos.map(f => ({ id: f.id, url: f.url, alt: f.alt }))`
  - `handleAdd(urls: string[])`: call `createAlojamientoFoto({ alojamientoId: alojamiento.id, url, alt: "Foto N", orden: fotos.length + i })` for each url
  - `handleRemove(id: string)`: call `deleteAlojamientoFoto(id)`
  - `handleReorder(reordered: ImageItem[])`: call `updateAlojamientoFoto({ ...original, orden: newIndex })` for each item where orden changed
  - `<ImageUploader images={images} onAdd={canEdit ? handleAdd : undefined} onRemove={canEdit ? handleRemove : undefined} onReorder={canEdit ? handleReorder : undefined} />`
  - Import `ImageItem` type from `@/components/ui/ImageUploader` (or define locally if not exported)
- Card title: "Fotos del Hotel"
- VENDEDOR: ImageUploader still shows photos (read-only via undefined callbacks), no upload/remove controls visible

All three cards use `glassMaterials.frosted` inline style for the wrapper divs. Maintain consistent spacing (`space-y-6` or equivalent) between cards.
  </action>
  <verify>
    Navigate to /alojamientos/{existing-id} — three-card layout renders: hotel form, price table, photo grid. Edit nombre, save → toast. Price table shows seed rows with regimen names. Click Edit on a price row → regimen shows as Select dropdown, period fields become date inputs. Save → row returns to view with regimen name. Add periodo → row appears, fill all fields including regimen, save → new row in table. Delete row → removed with toast. Photo grid shows seed photos; upload new image → appears in grid; drag to reorder; remove photo → disappears. VENDEDOR: all action controls absent, photos still visible.
  </verify>
  <done>
    Alojamiento detail renders all three sections; hotel form edits and saves; price table inline editing with regimen select works correctly including state reset; photo grid supports add/remove/reorder via ImageUploader; VENDEDOR sees no controls.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` — no TypeScript errors in alojamientos pages
2. Navigate to /alojamientos — glass table renders with all columns, star ratings show correctly for each hotel's categoria value
3. Ciudad and Pais columns show resolved names (not raw UUIDs)
4. Navigate to /alojamientos/nuevo — pais select populates; selecting a pais loads ciudad options; changing pais resets ciudad
5. Navigate to /alojamientos/{valid-uuid} — all three cards render
6. Inline price table: edit row with regimen select → save → returns to view mode
7. Add precio row: new row with regimen → saves to provider state
8. Photo grid: existing fotos appear; upload and remove work
9. Confirm static /alojamientos/nuevo route is not captured by [id] dynamic segment
10. VENDEDOR: no action buttons, no upload controls, no editable inputs
</verification>

<success_criteria>
- Alojamientos list shows all hotel fields with correct ciudad/pais name resolution and star ratings
- Search filters by nombre, ciudad name, pais name
- Clone creates "Copia de {nombre}" with toast; delete removes via shake modal with toast
- /alojamientos/nuevo renders with cascading pais→ciudad selects; ciudad resets when pais changes
- /alojamientos/[id] renders form + price table + photo grid in three-card vertical layout
- Price table edit: all three column types editable (date, number, select)
- Regimen Select shows regimen names in dropdown during edit mode
- Price row state resets correctly after save/cancel (no stuck edit state)
- Photo grid: add/remove/reorder all functional via ImageUploader
- TypeScript compiles clean (no `any` types, correct PrecioAlojamiento shape)
</success_criteria>

<output>
After completion, create `.planning/phases/05-aereos-alojamientos/05-02-SUMMARY.md` using the summary template.
</output>
