---
phase: 05-aereos-alojamientos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/aereos/page.tsx
  - src/app/(admin)/aereos/nuevo/page.tsx
  - src/app/(admin)/aereos/[id]/page.tsx
autonomous: true
requirements: [AERO-01, AERO-02, AERO-03, AERO-04]

must_haves:
  truths:
    - "Aereos list renders glass table with columns: ID, ruta, destino, acciones"
    - "User can create an aereo via /aereos/nuevo — navigates to aereos list on save"
    - "User can clone an aereo — a copy with 'Copia de {ruta}' prefix appears in the list with a success toast"
    - "User can delete an aereo via shake-animated modal — row disappears with success toast"
    - "Aereo detail page shows all flight fields (ruta, destino, aerolinea, equipaje) in an editable card"
    - "Detail page shows a price-per-period table with rows for periodoDesde, periodoHasta, precioAdulto, precioMenor"
    - "User can click Edit on a price row — cells become inputs, Save/Cancel buttons appear"
    - "User can save an edited price row — row returns to view mode, context updated, toast shown"
    - "User can add a new price row via '+ Agregar periodo' button — new empty row in edit mode appears at bottom"
    - "User can delete a price row directly — row disappears with toast (no confirmation modal)"
    - "VENDEDOR role sees no action buttons and no editable fields"
  artifacts:
    - path: "src/app/(admin)/aereos/page.tsx"
      provides: "Aereos list with glass table, search, clone, delete modal"
      min_lines: 160
    - path: "src/app/(admin)/aereos/nuevo/page.tsx"
      provides: "Create aereo form — static route before [id] to avoid Next.js capture"
      min_lines: 60
    - path: "src/app/(admin)/aereos/[id]/page.tsx"
      provides: "Aereo detail with editable form + inline price table"
      min_lines: 200
  key_links:
    - from: "src/app/(admin)/aereos/page.tsx"
      to: "useAereos(), useServiceActions()"
      via: "ServiceProvider hooks"
      pattern: "useAereos|useServiceActions"
    - from: "src/app/(admin)/aereos/[id]/page.tsx"
      to: "useServiceState().preciosAereo"
      via: "filter by aereoId"
      pattern: "preciosAereo\\.filter\\(p => p\\.aereoId"
    - from: "inline price table save handler"
      to: "createPrecioAereo / updatePrecioAereo"
      via: "useServiceActions()"
      pattern: "createPrecioAereo|updatePrecioAereo"
---

<objective>
Build the complete Aereos module: list page with glass table and full CRUD, a /aereos/nuevo create page, and a detail/edit page at /aereos/[id] with an inline-editable price-per-period table.

Purpose: Users can browse, search, create, edit, clone, and delete flight entities. The detail page reveals per-period pricing (periodoDesde, periodoHasta, precioAdulto, precioMenor) with inline editing — the core new UI pattern for Phase 5.
Output: Three page files replacing the placeholder and adding new routes for the Aereos module.
</objective>

<execution_context>
@/Users/diegothx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/diegothx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-aereos-alojamientos/05-RESEARCH.md

# Key source files to mirror
@src/app/(admin)/paquetes/page.tsx
@src/app/(admin)/paquetes/[slug]/_components/DatosTab.tsx
@src/components/providers/ServiceProvider.tsx
@src/lib/types.ts
@src/components/lib/animations.ts
@src/components/lib/glass.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aereos list page and /aereos/nuevo create page</name>
  <files>
    src/app/(admin)/aereos/page.tsx
    src/app/(admin)/aereos/nuevo/page.tsx
  </files>
  <action>
Replace the placeholder `src/app/(admin)/aereos/page.tsx` with a full list page mirroring `src/app/(admin)/paquetes/page.tsx`. Key differences from the Paquetes list:
- Import `useAereos` (not `usePaquetes`) from ServiceProvider
- Import `createAereo`, `deleteAereo` from `useServiceActions()`
- No filter chips — search-only (search across `ruta` and `destino` fields)
- Table columns: ID (short UUID prefix), Ruta, Destino, Acciones (Eye, Pencil, Copy, Trash2 icon buttons)
- Clone handler: copies all Aereo fields, prefixes ruta with `"Copia de "`, calls `createAereo({brandId, ruta, destino, aerolinea, equipaje})`, shows success toast
- Delete modal: identical shake pattern to Paquetes (uses `interactions.deleteShake` from `@/components/lib/animations`, spread `[...interactions.deleteShake.animate.x]` for TS compat), calls `deleteAereo(id)` after animation, shows toast
- VENDEDOR: canEdit gates Nuevo Aereo button and all row action buttons (Eye/Pencil still shown for navigation, but Copy/Trash2 hidden when !canEdit)
- Pagination: include `Pagination` component at 10 items/page
- PageHeader: title "Aereos", icon `Plane` from lucide-react
- Empty state: centered `Plane` icon with text "No hay vuelos registrados"
- Navigation: Eye → `/aereos/{id}`, Pencil → `/aereos/{id}`, New → `/aereos/nuevo`

Create `src/app/(admin)/aereos/nuevo/page.tsx` as a static route (MUST exist before `[id]` to avoid Next.js route capture). Mirror `src/app/(admin)/paquetes/nuevo/page.tsx`:
- VENDEDOR guard: `useEffect` redirect to `/aereos` if !canEdit
- Form fields: ruta (text input), destino (text input), aerolinea (text input), equipaje (text input, e.g., "23kg bodega")
- Default values: empty strings for all fields
- On save: call `createAereo({brandId: activeBrandId, ruta, destino, aerolinea, equipaje})`, capture returned id from state change (use `useServiceState().aereos` length change or redirect to `/aereos` on success — simpler: redirect to `/aereos` after create, not to `/aereos/{id}`, to stay consistent with simplicity of this module)
- Cancel: navigate back to `/aereos`
- Glass card layout with inline style from `glassMaterials.frosted`, consistent with DatosTab styling
- Use `"use client"` directive
  </action>
  <verify>
    Navigate to /aereos — glass table renders with ID, Ruta, Destino, Acciones columns and existing seed data rows. Search for "Buenos" filters results. Clone row → "Copia de {ruta}" appears in table with toast. Delete modal opens with shake on confirm button. Navigate to /aereos/nuevo — form renders (not a 404 or "aereo not found" error).
  </verify>
  <done>
    Aereos list shows all brand-filtered aereos in glass table; search works; clone/delete work with toasts; /aereos/nuevo renders the create form; VENDEDOR sees no Nuevo/Copy/Trash buttons.
  </done>
</task>

<task type="auto">
  <name>Task 2: Aereo detail page with editable form and inline price-per-period table</name>
  <files>
    src/app/(admin)/aereos/[id]/page.tsx
  </files>
  <action>
Create `src/app/(admin)/aereos/[id]/page.tsx` — the detail/edit page for a single Aereo, consisting of two stacked cards:

**Card 1 — Flight data form:**
- Use `useParams<{ id: string }>()` to get the id
- Look up aereo: `useServiceState().aereos.find(a => a.id === id && !a.deletedAt)` — if not found, show "Aereo no encontrado" with back button
- Local `useState` for form fields initialized from aereo: `{ ruta, destino, aerolinea, equipaje }`
- Inputs for all 4 fields using glass input styling (consistent with DatosTab — inline style or `glassMaterials.input`)
- Save button calls `updateAereo({ ...aereo, ...formState })` then shows toast "Vuelo actualizado"
- Back button → `/aereos`
- PageHeader with title = aereo.ruta, breadcrumb path, back arrow
- VENDEDOR: all inputs read-only (`readOnly` prop), Save button hidden

**Card 2 — Inline price-per-period table:**
- Read prices: `useServiceState().preciosAereo.filter(p => p.aereoId === aereo.id)` — filter by parent ID (not by brand)
- Local state for inline editing:
  ```
  const [editingRowId, setEditingRowId] = useState<string | null>(null)
  const [draftRow, setDraftRow] = useState<Partial<PrecioAereo>>({})
  const [addingRow, setAddingRow] = useState(false)
  const [newRow, setNewRow] = useState<Partial<Omit<PrecioAereo, "id" | "aereoId">>>({})
  ```
- Table columns: Periodo Desde, Periodo Hasta, Neto Adulto (USD), Neto Menor (USD), Acciones
- **View mode row** (editingRowId !== row.id): show formatted values, Edit (Pencil) and Delete (Trash2) icon buttons
- **Edit mode row** (editingRowId === row.id): cells become `<input type="date">` for periodo fields and `<input type="number">` for precio fields, bound to `draftRow` state. Save (checkmark or Save text) and Cancel (X) buttons. On Save: call `updatePrecioAereo(draftRow as PrecioAereo)`, reset `editingRowId` to null AND `draftRow` to `{}` (reset BOTH to avoid desync), show toast "Precio actualizado". On Cancel: reset both state values.
- **Add row**: Persistent "+ Agregar periodo" button below table. Clicking sets `addingRow = true`, shows a new empty editable row at the bottom. On Save: call `createPrecioAereo({ aereoId: aereo.id, periodoDesde, periodoHasta, precioAdulto, precioMenor })`, set `addingRow = false`, reset `newRow`, show toast "Periodo agregado". On Cancel: set `addingRow = false`, reset `newRow`.
- **Delete row**: Call `deletePrecioAereo(id)` directly — no confirmation modal. Show toast "Periodo eliminado".
- VENDEDOR: Edit/Delete/Add buttons hidden; table is read-only
- Card title: "Precios por Periodo"
- Empty state when no precios: "Sin periodos definidos" text + Add button (if canEdit)
- Price formatting: display USD amounts with `formatCurrency` from `@/lib/utils` or raw number with "$" prefix
- Use glass card wrapper with `glassMaterials.frosted` inline style for card containers
  </action>
  <verify>
    Navigate to /aereos/{existing-id} — detail card shows ruta/destino/aerolinea/equipaje. Edit ruta, click Save — toast "Vuelo actualizado" appears. Price table shows existing seed rows. Click Edit on a row — cells become inputs. Change a value, click Save — row returns to view mode with updated value, toast shows. Click + Agregar periodo — empty row appears; fill dates and prices; click Save — row added, toast shows. Click Delete on a row — row removed immediately with toast. Navigate to /aereos/nuevo — form renders correctly (no "aereo not found").
  </verify>
  <done>
    Aereo detail renders flight data form and price-per-period table; inline row editing cycles correctly through view/edit modes with state reset after save/cancel; add/delete rows work; VENDEDOR sees read-only table with no action buttons.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` — no TypeScript errors in aereos pages
2. Navigate to /aereos — table renders with seed data (12 aereos)
3. Navigate to /aereos/nuevo — create form renders (not a route capture error)
4. Navigate to /aereos/{valid-uuid} — detail page renders with price table
5. Confirm price row inline edit: view → edit → save → view (no stuck edit state)
6. Confirm price row delete: row disappears with toast, no modal
7. Confirm clone on list: "Copia de {ruta}" appears with toast
8. Confirm VENDEDOR: login as VENDEDOR, navigate to /aereos — no action buttons visible
</verification>

<success_criteria>
- Glass table at /aereos shows ID, Ruta, Destino, Acciones columns with all seed aereos
- Search filters by ruta and destino in real time
- Clone creates "Copia de {ruta}" with toast
- Delete modal has shake animation, removes aereo with toast
- /aereos/nuevo renders create form (VENDEDOR redirected to /aereos)
- /aereos/[id] shows form + price table in two-card vertical layout
- Inline price editing: row enters edit mode, Save resets state, toast shows
- Add row: new row saves to ServiceProvider state
- Delete row: direct delete with toast, no modal
- TypeScript compiles clean (no `any` types)
</success_criteria>

<output>
After completion, create `.planning/phases/05-aereos-alojamientos/05-01-SUMMARY.md` using the summary template.
</output>
