# TravelOz — Changelog

## [2.3.0] — 2026-07-08 — Video en work-with-us, alineación del listing de región, robustez del newsletter (LGPD/GDPR) y pulido del admin

### Resumen
Tres tandas grandes. **Work-with-us:** video MP4 subido y rendereado en loop silencioso, con upload-only desde el admin (antes el admin confundía el MediaPicker con un input URL). **Listing de región:** la página `/destinos/<region>` ahora matchea el shell del reference (`html_inicial/destination-listing.html`) — h1 centrado, filter form, sort dropdown, grid 4-up en xxl — y suma un typeahead de ciudad + chips de temporada con filtro en tiempo real. **Newsletter:** captura metadata de consentimiento (IP, UA, UTM params) al opt-in, link de unsubscribe en cada email con headers RFC 8058, endpoint público `/newsletter/unsubscribe`, y la pantalla del admin muestra el detalle (IP/UA/UTM) en un popover. Cambios de DB: 1 migración nueva, 1 fila en `SiteSetting`. Todo no-destructivo y testeado de punta a punta contra producción.

---

### Work-with-us: video + upload-only

- **Video MP4 optimizado y subido al bucket** ([scripts/upload-workwithus-video.ts](../../scripts/upload-workwithus-video.ts), [scripts/set-workwithus-video.ts](../../scripts/set-workwithus-video.ts)): el `workwithus.mp4` del root era 80 MB en HEVC 3072×3840 — un tamaño absurdo para web. Recomprimido a 1080×1350 H.264 (libx264, CRF 23, +faststart) — bajó a **7.0 MB** sin pérdida visible. Subido al bucket como key estable `site/workwithus.mp4`. También queda en `public/site/video/workwithus.mp4` como asset estático.
- **Ruta pública con video en loop** ([work-with-us/page.tsx](../../src/app/(public)/work-with-us/page.tsx)): el `<video>` pasó a `autoplay muted loop playsInline preload="auto"` con `key={videoUrl}` para remount limpio cuando el admin cambia la URL. **Overlay oscuro eliminado** ([site.css](../../src/app/(public)/site.css)): `.work-with-img::after` (background `#282828` opacity 0.7) y `.video-placeholder img { filter: brightness(0.7) }` apagados dentro de `.work-with-us` para que el video se vea full-bright. La imagen fallback (cuando no hay video) mantiene el brillo normal.
- **Admin upload-only** ([work-with-us/page.tsx](../../src/app/(public)/work-with-us/page.tsx)): el `MediaPicker` de `workwithus_video_url` ahora pasa `variant="alt"` (es un `<input type="hidden">` puro, sin el toggle "Pegar URL externa") + `hideUrl: true`. Antes el admin veía el toggle y pensaba que el campo era solo URL.
- **Pipeline de upload acepta video** ([file-pipeline.ts](../../src/lib/file-pipeline.ts)): `ALLOWED_VIDEO_MIMES = { video/mp4, video/webm, video/quicktime }`, `MAX_BYTES` subido de 25 MB a **100 MB**, y `processAndUpload` ahora distingue imágenes / videos / documentos para saltarse el pipeline de sharp en videos (sharp no puede transcodear y corrompería el MP4).
- **Fallback de imagen rota en testimonios** ([HomeTestimonios.tsx](../../src/components/public/HomeTestimonios.tsx)): `t.imageUrl?.trim() || fallback` + `onError` que cae al mismo fallback. Antes `??` no atrapaba el string vacío y mostraba el iconito de imagen rota cuando el admin subía sin foto.

---

### Listing de región: alineación con reference + filtros en tiempo real

- **Shell del reference restaurado** ([RegionExplorer.tsx](../../src/components/public/RegionExplorer.tsx)): `<section.listing-area>` con fondo `#F4F4F4`, `<div.container.wide>`, `<div.listing-filter>` con `<h1.h2.text-center>Explorá todos los destinos</h1>`, `<div.filter-form>` con 2 inputs (Destino + Temporada) + botón Buscar con icono. Sort dropdown custom `.cs_dropdown` con Menor/Mayor precio. Grid `col-xxl-3 col-lg-4 col-sm-6` con gutter `g-lg-4 g-3` (4-up en viewports >1400px, 3-up en lg, 2-up en sm).
- **Card del listing pasa a `variant="alt"`** ([PackageCard.tsx](../../src/components/public/PackageCard.tsx)): nuevo prop opcional `variant?: "default" | "alt"`. `alt` aplica `className="box-card alt"` (imagen 240px + `height:100%` para alinear cards en la misma fila). `RelatedPackages` y `/destinos?tipo=` siguen con `default` (280px) — no se tocan.
- **Tipografía del card arreglada** ([site.css](../../src/app/(public)/site.css)): `.box-card .text>p { font-size: 14px; }`. Antes el `<p>Octubre - Noviembre 2026</p>` heredaba 18px del global. Ahora matchea el reference.
- **Bullets en el `<ul>` de "Incluye"** ([site.css](../../src/app/(public)/site.css)): `list-style: disc; padding-left: 20px;`. Bootstrap reset + Tailwind preflight los borraban. Afecta a los 3 consumidores de `PackageCard` (RegionExplorer, RelatedPackages, /destinos?tipo=).
- **Helper render-side `stripNochesSuffix`** ([format-paquete.ts](../../src/lib/format-paquete.ts)): 48 de 85 paquetes tienen `- NN Noches` concatenado en el `titulo` de la DB. El helper regex strippea el sufijo en los 3 renderers públicos de grid. **No toca la DB.** El detail view sigue usando `splitTitulo` y muestra las noches a propósito en una segunda línea.
- **Typeahead de ciudad (multi)** ([RegionExplorer.tsx](../../src/components/public/RegionExplorer.tsx)): input con icono location, dropdown con ciudades matcheadas por nombre o país. Cada ciudad elegida se ve como chip removable adentro del input. Enter acepta la primera sugerencia, Backspace borra el último chip. Sugerencias excluyen las ya elegidas.
- **Input-dropdown de temporada (multi)** ([RegionExplorer.tsx](../../src/components/public/RegionExplorer.tsx)): input con icono file, dropdown con los 12 meses en español (incluye "setiembre" como alias de "septiembre") parseados de `paquete.salidas`. Click en un mes lo toggle, check ✓ al lado. Solo se muestran los meses que algún paquete de la región menciona.
- **Filtro en tiempo real** (ciudades AND temporadas) + sort independiente. Empty state distinto si hay filtros: "No hay paquetes que coincidan con los filtros en {región}.".
- **Chips más lindos** ([site.css](../../src/app/(public)/site.css)): violeta sólido `#785AE5` con texto blanco, `border-radius: 6px`, alto fijo 28px, botón X 18×18 con hover. Reemplazó la versión lavanda apagada anterior que el cliente veía como "redondeles feos".
- **Dropdown re-abrible** ([RegionExplorer.tsx](../../src/components/public/RegionExplorer.tsx)): `onClick` además de `onFocus` para reabrir cuando el input ya tiene focus. Antes había que clickear afuera primero.
- **Botón "Limpiar filtros"** a la izquierda del sort, solo visible cuando hay algo activo.

---

### Newsletter: email configurable del admin + LGPD/GDPR

- **Email configurable del admin** ([public-forms.actions.ts](../../src/actions/public-forms.actions.ts), [key-validators.ts](../../src/app/backend/web/_components/key-validators.ts), [seed-public.ts](../../prisma/seed-public.ts)): nueva key `notificaciones_email_newsletter` en el grupo "notificaciones". Mismo patrón que los otros 4 forms (`contacto`, `corporativo`, `cotizar`, `trabaja`): aparece en `/backend/web/notificaciones` con el `EmailTagInput` (chips removibles, multi-destino por coma). El `submitNewsletterForm` ahora dispara `notifyLead({ settingKey: "notificaciones_email_newsletter", ... })` con `Reply-To` puesto al email del suscriptor. **Fire-and-forget** (`void`) para no bloquear al visitante si Resend está caído. Fila creada en producción vía one-shot script.

#### LGPD / GDPR / Ley 18.331 (UY): metadata de consentimiento

- **Migración nueva** ([prisma/migrations/20260708183554_newsletter_consent_metadata](../../prisma/migrations/20260708183554_newsletter_consent_metadata/)): 7 columnas nuevas en `SuscripcionNewsletter` — `consentIp`, `consentUserAgent`, `utmSource`, `utmMedium`, `utmCampaign`, `utmContent`, `utmTerm`. Aplicada a producción.
- **Captura de UTM + UA en el cliente** ([HomeNewsletter.tsx](../../src/components/public/HomeNewsletter.tsx)): server actions de Next 14 no exponen los headers HTTP del request original, así que la metadata se captura en el cliente (`window.location.search` para UTMs, `navigator.userAgent` para UA) y se pasa por FormData con prefijo `_`. El server action la lee de ahí. La IP se sigue capturando del header `x-forwarded-for` (sí accesible desde server actions).
- **No destructivo en re-suscripciones**: el `upsert.update` solo refresca `active`, `confirmToken`, `confirmedAt` y `unsubscribedAt`. Los datos de consentimiento del primer opt-in (IP/UA/UTMs) son el registro histórico y no se pisan.

#### Unsubscribe público + headers RFC 8058

- **`/newsletter/unsubscribe?email=…`** ([unsubscribe/page.tsx](../../src/app/(public)/newsletter/unsubscribe/page.tsx)): endpoint público sin token. La única forma de generar el link es abriendo el email que se mandó a esa dirección, así que el visitante ya está autenticado. 4 outcomes: `ok` (marca `active=false` + `unsubscribedAt=now()` + invalida `confirmToken` si estaba pendiente), `already` (ya dado de baja), `notfound` (email no existe — no revela nada), `invalid` (sin param).
- **Link "Cancelar suscripción" en el footer del email** ([email.ts](../../src/lib/email.ts)): `brandedLayout()` ahora acepta `unsubscribeUrl` opcional y lo renderea como link discreto en el footer gris.
- **Headers RFC 8058 en `sendEmail()`** ([email.ts](../../src/lib/email.ts)): cuando se pasa `unsubscribeUrl`, se agregan `List-Unsubscribe: <mailto:unsub@traveloz.com.uy>, <https://…>` y `List-Unsubscribe-Post: List-Unsubscribe=One-Click`. Gmail/Outlook/Apple Mail muestran el botón nativo de "Cancelar suscripción" arriba del email. Los emails transaccionales al admin (leads) no llevan estos headers (no son emails de lista).

#### Admin: ver el detalle de consentimiento

- **Popover de detalle en `/backend/leads/newsletter`** ([newsletter/page.tsx](../../src/app/backend/leads/newsletter/page.tsx)): nueva columna "Datos" con ícono `Info`. Al click abre un popover (React stateful, click-away + Escape) que muestra IP, UA (truncado a 80 chars), los 5 UTM params, fecha de confirmación y fecha de baja. Si el row no tiene ninguno de esos campos (rows viejos, antes de la migración), muestra un guion `—` en vez del botón.
- **CSV export ampliado** ([leads.actions.ts](../../src/actions/leads.actions.ts)): 6 columnas → **14 columnas**: ID, Fecha alta, Email, Activo, Origen, Confirmado, Fecha baja, IP, User Agent, utm_source, utm_medium, utm_campaign, utm_content, utm_term.

---

### Footer: link "Cotizá tu viaje"

- **Link agregado** ([set-footer-cotizar-link.ts](../../scripts/set-footer-cotizar-link.ts)): la columna 3 del footer (`footer_links_json`) ahora tiene "Cotizá tu viaje" como primer item, llevando a `/cotizar`. **DB-only**: no toqué el seed porque el cliente ya había editado el JSON a mano y el seed usa `update: {}`.

---

### Admin: email de notificaciones con tag input

- **`EmailTagInput` component** ([EmailTagInput.tsx](../../src/components/ui/EmailTagInput.tsx)): chips removibles para los campos `notificaciones_email_*` del admin. Cada chip es violeta sólido `#785AE5` con texto blanco, `border-radius: 6px`, alto 28px. Botón X 18×18 con hover. Acepta paste multi-email (auto-split por coma/espacio). Enter agrega el chip pendiente. Backspace borra el último. Afecta a `notificaciones_email_contacto`, `_corporativo`, `_cotizacion`, `_trabaja`, `_newsletter`.

---

### Notas de producción

- **Cambios de DB aplicados via migración o script one-shot**:
  - Migración `20260708183554_newsletter_consent_metadata` (7 columnas nuevas en `SuscripcionNewsletter`).
  - Fila `notificaciones_email_newsletter` en `SiteSetting` (one-shot script `add-newsletter-notification.ts`).
  - `workwithus_video_url` → `/api/image/site/workwithus.mp4` (one-shot `set-workwithus-video.ts`).
  - `footer_links_json` con "Cotizá tu viaje" primero (one-shot `set-footer-cotizar-link.ts`).
  - Key `site/workwithus.mp4` subida al bucket Tigris de Traveloz (one-shot `upload-workwithus-video.ts`).
- **Cachés públicos**: la mayoría de páginas públicas tienen `revalidate` de 60–300s, así que los cambios de contenido terminan de propagarse en pocos minutos. El newsletter y el upload de video no afectan caché.
- **No se tocó Destinico**: el path de las migraciones/scripts apunta solo a `traveloz-admin` y el bucket es el de TravelOz.
- **Tests E2E ejecutados contra producción** (sin destructive):
  - Newsletter: submit → DB row insertado con UTM/IP/UA → confirm → row activado → unsubscribe → row dado de baja.
  - Listing: typeahead de ciudad + chips de temporada en Brasil (1 paquete, 2 ciudades, 2 meses).
  - Work-with-us: video MP4 servido con `Content-Type: video/mp4` y cache 1 año.
  - Admin: CSV export con las 14 columnas nuevas.
