# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# railway
.railway

# typescript
*.tsbuildinfo
next-env.d.ts

# claude code (internal state, not project)
.claude/
.playwright-mcp/

# Playwright/debug screenshots (never commit)
/*.png
/*.jpeg
/*.jpg

# QA / audit screenshots (kept locally, not in repo)
audit/

# Artefactos locales y PDFs de diseño (no versionar)
/*.pdf

# El proyecto usa npm (Railway/nixpacks corre `npm run build`). Estos archivos
# pnpm quedaron de un experimento local; versionarlos haría que nixpacks
# cambie a pnpm y rompa el build de producción.
pnpm-lock.yaml
pnpm-workspace.yaml
